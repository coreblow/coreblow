import fs from 'node:fs'; import path from 'node:path'; const MAX = 500; function check(dir: string) { const files = fs.readdirSync(dir, {recursive: true}).filter((f: any) => f.endsWith('.ts')); for (const f of files) { const loc = fs.readFileSync(path.join(dir, f as string), 'utf-8').split('\n').length; if (loc > MAX) console.warn(`⚠ ${f}: ${loc} lines (max ${MAX})`); } } check('src');
