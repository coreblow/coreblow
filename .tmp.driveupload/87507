/**
 * agents/turn-engine/channels/routing.ts
 * Channel routing logic for the turn engine.
 */
const channelRoutes = [];
export function addChannelRoute(route) {
    channelRoutes.push(route);
    channelRoutes.sort((a, b) => (b.priority ?? 0) - (a.priority ?? 0));
}
export function resolveChannelRoute(channel, content) {
    for (const route of channelRoutes) {
        if (route.channel !== channel)
            continue;
        if (route.filter && content && !route.filter({ content, channel }))
            continue;
        return route.agentId;
    }
    return null;
}
export function clearChannelRoutes() {
    channelRoutes.length = 0;
}
//# sourceMappingURL=routing.js.map
