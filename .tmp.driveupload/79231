/**
 * cli/progress.ts
 * Terminal progress indicators (spinner, progress bar).
 * Ported from CoreBlow reference cli-utils + progress patterns.
 */
export declare class Spinner {
    private frameIndex;
    private interval;
    private message;
    private stream;
    constructor(message: string, stream?: NodeJS.WriteStream);
    start(): this;
    update(message: string): void;
    succeed(message?: string): void;
    fail(message?: string): void;
    warn(message?: string): void;
    stop(finalMessage?: string): void;
    private render;
}
export declare class ProgressBar {
    private current;
    private total;
    private width;
    private label;
    private stream;
    constructor(label: string, total: number, width?: number, stream?: NodeJS.WriteStream);
    update(current: number): void;
    increment(amount?: number): void;
    finish(message?: string): void;
}
/**
 * Run a task with a spinner.
 */
export declare function withSpinner<T>(message: string, fn: (spinner: Spinner) => Promise<T>): Promise<T>;
//# sourceMappingURL=progress.d.ts.map
