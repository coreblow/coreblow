/**
 * full-plugin — Commands
 */

export const statusCommand = {
    name: '/full-plugin',
    description: 'full-plugin status',
    async handler(args?: string[]) {
        return 'full-plugin is running!';
    },
};
