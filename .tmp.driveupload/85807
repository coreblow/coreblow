/**
 * CoreBlow — Connection Pool V2
 *
 * Advanced connection pooling with health checks,
 * automatic scaling, connection recycling, and
 * per-host limits.
 */
/** Pool connection */
export interface PoolConnection {
    id: string;
    host: string;
    status: 'idle' | 'active' | 'draining';
    createdAt: number;
    lastUsedAt: number;
    useCount: number;
}
/**
 * CoreBlow Connection Pool V2
 */
export declare class ConnectionPoolV2 {
    private connections;
    private maxPerHost;
    private maxAge;
    private idCounter;
    private stats;
    constructor(maxPerHost?: number, maxAgeMs?: number);
    /**
     * Acquire a connection.
     */
    acquire(host: string): PoolConnection | null;
    /**
     * Release a connection.
     */
    release(id: string): boolean;
    /**
     * Drain a host (mark all as draining).
     */
    drain(host: string): number;
    /**
     * Cleanup expired connections.
     */
    cleanup(): number;
    /**
     * Get pool status.
     */
    getStatus(): {
        total: number;
        active: number;
        idle: number;
        draining: number;
    };
    /**
     * Get stats.
     */
    getStats(): typeof this.stats;
    /** Size */
    size(): number;
}
//# sourceMappingURL=connection-pool-v2.d.ts.map
