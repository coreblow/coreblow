import AppIntents

struct SummarizeIntent: AppIntent {
    static var title: LocalizedStringResource = "Summarize"
    static var description = IntentDescription("CoreBlow Summarize")

    func perform() async throws -> some IntentResult {
        return .result()
    }
}
