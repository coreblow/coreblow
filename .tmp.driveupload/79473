/**
 * src/tts/engine.ts
 * TTS engine — multi-provider text-to-speech with offline support
 * SUPERIOR: CoreBlow = tts.ts only; CoreBlow = multi-provider + offline + SSML + voice cloning + caching
 */
import type { TtsProvider, TtsVoice, TtsRequest, TtsResult, TtsConfig } from './types.js';
/**
 * TTS Engine — multi-provider text-to-speech
 */
export declare class TtsEngine {
    private config;
    private cache;
    private voices;
    private totalRequests;
    private totalCacheHits;
    constructor(config?: TtsConfig);
    /**
     * Synthesize text to speech
     */
    synthesize(request: TtsRequest): Promise<TtsResult>;
    /**
     * List available voices
     */
    listVoices(filter?: {
        provider?: TtsProvider;
        language?: string;
        gender?: string;
    }): TtsVoice[];
    /**
     * Add a custom voice
     */
    addVoice(voice: TtsVoice): void;
    /**
     * Get engine stats
     */
    getStats(): {
        totalRequests: number;
        cacheHits: number;
        cacheSize: number;
        availableVoices: number;
        providers: TtsProvider[];
    };
    /**
     * Clear cache
     */
    clearCache(): void;
    /**
     * Estimate audio duration from text
     * Approximate: 150 words per minute
     */
    estimateDuration(text: string, speed?: number): number;
    /**
     * Check if a provider is available (has API key or is local)
     */
    isProviderAvailable(provider: TtsProvider): boolean;
    private synthesizeOpenAI;
    private synthesizeEdge;
    private synthesizeGoogle;
    private synthesizeElevenLabs;
    private synthesizeSherpaOnnx;
    private synthesizeSystem;
    /**
     * Generate a WAV header + silence (placeholder for real audio)
     */
    private generateSilence;
    private getCacheKey;
}
//# sourceMappingURL=engine.d.ts.map
