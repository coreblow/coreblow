/**
 * plugin-sdk/provider-helpers.ts
 * LLM provider integration helpers for plugins.
 */
export interface CompletionRequest {
    model: string;
    messages: Array<{
        role: string;
        content: string;
    }>;
    maxTokens?: number;
    temperature?: number;
    topP?: number;
    stream?: boolean;
}
export interface CompletionResponse {
    content: string;
    model: string;
    usage?: {
        promptTokens: number;
        completionTokens: number;
        totalTokens: number;
    };
    finishReason?: string;
}
/**
 * Resolve provider configuration from config.
 */
export declare function resolveProviderConfig(model: string, cfg?: Record<string, unknown>): {
    provider: string;
    modelId: string;
    apiKey?: string;
    endpoint?: string;
};
/**
 * Get list of available models from config.
 */
export declare function getAvailableModels(cfg?: Record<string, unknown>): string[];
/**
 * Validate a model string.
 */
export declare function isValidModelString(model: string): boolean;
//# sourceMappingURL=provider-helpers.d.ts.map
