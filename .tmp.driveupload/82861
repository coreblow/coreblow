/**
 * auto-reply/thinking.ts
 * Thinking status and reasoning level management.
 * Ported from CoreBlow reference src/auto-reply/thinking.ts.
 */
const THINKING_CATALOG = [
    { level: 'none', label: 'None', description: 'No thinking/reasoning tokens' },
    { level: 'low', label: 'Low', description: 'Minimal thinking tokens', tokenBudget: 1024 },
    { level: 'medium', label: 'Medium', description: 'Moderate thinking tokens', tokenBudget: 4096 },
    { level: 'high', label: 'High', description: 'Extended thinking tokens', tokenBudget: 16384 },
    { level: 'max', label: 'Max', description: 'Maximum thinking tokens', tokenBudget: 65536 },
];
export function normalizeThinkLevel(raw) {
    const v = raw?.trim().toLowerCase();
    if (v === 'none' || v === 'off' || v === '0')
        return 'none';
    if (v === 'low' || v === 'min' || v === '1')
        return 'low';
    if (v === 'medium' || v === 'med' || v === '2')
        return 'medium';
    if (v === 'high' || v === '3')
        return 'high';
    if (v === 'max' || v === 'xhigh' || v === '4')
        return 'max';
    return undefined;
}
export function normalizeReasoningLevel(raw) {
    return normalizeThinkLevel(raw);
}
export function normalizeElevatedLevel(raw) {
    const v = raw?.trim().toLowerCase();
    if (v === 'standard' || v === 'normal')
        return 'standard';
    if (v === 'elevated' || v === 'high')
        return 'elevated';
    return undefined;
}
export function normalizeVerboseLevel(raw) {
    const v = raw?.trim().toLowerCase();
    if (v === 'off' || v === 'none')
        return 'off';
    if (v === 'brief' || v === 'short')
        return 'brief';
    if (v === 'full' || v === 'verbose')
        return 'full';
    return undefined;
}
export function normalizeUsageDisplay(raw) {
    const v = raw?.trim().toLowerCase();
    if (v === 'off' || v === 'none')
        return 'off';
    if (v === 'brief' || v === 'short')
        return 'brief';
    if (v === 'full' || v === 'verbose')
        return 'full';
    return undefined;
}
export function normalizeFastMode(raw) {
    const v = raw?.trim().toLowerCase();
    if (v === 'true' || v === 'on' || v === '1')
        return true;
    if (v === 'false' || v === 'off' || v === '0')
        return false;
    return undefined;
}
export function listThinkingLevels() {
    return [...THINKING_CATALOG];
}
export function formatThinkingLevels() {
    return THINKING_CATALOG.map((e) => `  ${e.level}: ${e.description}`).join('\n');
}
/**
 * Check if a provider only supports binary thinking (on/off vs levels).
 */
export function isBinaryThinkingProvider(provider) {
    const p = provider?.trim().toLowerCase();
    if (!p)
        return false;
    const binary = ['anthropic', 'claude', 'deepseek'];
    return binary.some((b) => p.includes(b));
}
/**
 * Resolve the default thinking level for a model.
 */
export function resolveThinkingDefaultForModel(model) {
    if (!model)
        return 'none';
    const m = model.toLowerCase();
    if (m.includes('o1') || m.includes('o3'))
        return 'high';
    if (m.includes('claude-3-5') || m.includes('claude-4'))
        return 'medium';
    if (m.includes('gemini-2.5'))
        return 'medium';
    return 'none';
}
/**
 * Resolve token budget for a thinking level.
 */
export function resolveThinkingTokenBudget(level) {
    return THINKING_CATALOG.find((e) => e.level === level)?.tokenBudget;
}
//# sourceMappingURL=thinking.js.map
