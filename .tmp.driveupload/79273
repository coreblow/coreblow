/**
 * CoreBlow Skills Framework — Type Definitions
 *
 * Defines the metadata and runtime structures for dynamically loaded skills.
 * Skills are markdown files with YAML frontmatter that describe agent capabilities,
 * optionally backed by executable handler modules.
 */
/** Parsed YAML frontmatter from a SKILL.md file */
export interface SkillMetadata {
    /** Human-readable display name */
    name: string;
    /** Short description of what this skill does */
    description: string;
    /** Events this skill hooks into (e.g., 'message', 'tool-call') */
    events?: string[];
    /** Path to the handler module (relative to skill directory) */
    handler?: string;
    /** Named export to use from the handler module (defaults to 'default') */
    export?: string;
    /** Whether this skill is always active regardless of agent config */
    always?: boolean;
    /** Operating system requirements (e.g., ['darwin', 'linux']) */
    os?: string[];
    /** External binary dependencies required */
    requires?: string[];
    /** Optional emoji icon for display */
    emoji?: string;
}
/** A discovered skill entry with its file location and parsed metadata */
export interface SkillEntry {
    /** Unique identifier derived from directory name */
    id: string;
    /** Absolute path to the skill's root directory */
    baseDir: string;
    /** Absolute path to the SKILL.md file */
    markdownPath: string;
    /** The raw markdown content (instructions for the agent) */
    instructions: string;
    /** Parsed frontmatter metadata */
    metadata: SkillMetadata;
    /** Where this skill was discovered from */
    source: 'bundled' | 'workspace' | 'remote';
}
/** Runtime handler function signature for event-driven skills */
export type SkillHandler = (context: SkillContext) => Promise<string | void>;
/** Context passed to skill handlers at invocation time */
export interface SkillContext {
    /** The event that triggered this skill */
    event: string;
    /** The input message or payload */
    input: string;
    /** Agent session identifier */
    sessionId?: string;
    /** Additional metadata */
    extra?: Record<string, unknown>;
}
//# sourceMappingURL=types.d.ts.map
