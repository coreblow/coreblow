import type { FallbackAttempt, FallbackEntry, FallbackChain } from './model-fallback.types.js';
export type { FallbackEntry, FallbackChain } from './model-fallback.types.js';
/**
 * Thrown when all model fallback candidates have been exhausted.
 * Carries per-attempt details so callers can build informative
 * user-facing messages (e.g. "rate-limited, retry in 30s").
 *
 * @see openclaw-main/src/agents/model-fallback.ts
 */
export declare class FallbackSummaryError extends Error {
    readonly attempts: FallbackAttempt[];
    readonly soonestCooldownExpiry: number | null;
    constructor(message: string, attempts: FallbackAttempt[], soonestCooldownExpiry: number | null, cause?: Error);
}
export declare function isFallbackSummaryError(err: unknown): err is FallbackSummaryError;
export declare function createFallbackChain(entries: FallbackEntry[]): FallbackChain;
export declare function getCurrentModel(chain: FallbackChain): FallbackEntry | null;
export declare function advanceFallback(chain: FallbackChain, reason: string): FallbackEntry | null;
export declare function resetFallbackChain(chain: FallbackChain): void;
/**
 * @deprecated Prefer resolveFailoverReasonFromError() from failover-error.ts
 * which handles symbolic codes, HTTP status, Node codes, and cause chains.
 */
export declare function isRateLimitError(error: unknown): boolean;
//# sourceMappingURL=model-fallback.d.ts.map
