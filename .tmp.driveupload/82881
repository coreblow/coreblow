/**
 * auto-reply/send-policy.ts
 * Reply delivery policy engine.
 * Ported from CoreBlow reference src/auto-reply/send-policy.ts.
 */
export type SendPolicyOverride = 'allow' | 'deny';
export declare function normalizeSendPolicyOverride(raw?: string | null): SendPolicyOverride | undefined;
export declare function parseSendPolicyCommand(raw?: string): {
    hasCommand: boolean;
    mode?: SendPolicyOverride | 'inherit';
};
export interface CooldownState {
    lastReplyAt: number;
    cooldownMs: number;
}
/**
 * Check if a reply should be sent based on cooldown and rate limiting.
 */
export declare function shouldSendReply(params: {
    sendPolicy?: SendPolicyOverride;
    cooldown?: CooldownState;
    rateLimitPerMinute?: number;
    recentReplyCount?: number;
}): {
    allowed: boolean;
    reason?: string;
};
/**
 * Resolve send policy from config.
 */
export declare function resolveSendPolicy(cfg: Record<string, unknown>, channel?: string): {
    cooldownMs: number;
    rateLimitPerMinute: number;
};
//# sourceMappingURL=send-policy.d.ts.map
