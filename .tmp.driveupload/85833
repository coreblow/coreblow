/**
 * infra/tailscale.ts — Tailscale Integration for CoreBlow
 *
 * Provides Tailscale binary discovery, whois identity lookup (with cache),
 * serve/funnel management, and hostname resolution.
 *
 * Ported from CoreBlow reference src/infra/tailscale.ts (501 LOC) — streamlined
 * for CoreBlow architecture (simplified exec wrapper, no CLI prompts).
 */
export type TailscaleWhoisIdentity = {
    login: string;
    name?: string;
};
export type TailscaleMode = 'off' | 'serve' | 'funnel';
/**
 * Find the Tailscale binary. Tries:
 * 1. $COREBLOW_TAILSCALE_BINARY env override
 * 2. `which tailscale`
 * 3. Known macOS app path
 */
export declare function findTailscaleBinary(): Promise<string | null>;
export declare function getTailscaleBinary(): Promise<string>;
/**
 * Get the Tailscale DNS hostname or IP for this machine.
 */
export declare function getTailnetHostname(): Promise<string>;
/**
 * Look up a Tailscale identity by IP address.
 * Results are cached for 60s (success) or 5s (error).
 */
export declare function readTailscaleWhoisIdentity(ip: string, opts?: {
    timeoutMs?: number;
    cacheTtlMs?: number;
    errorTtlMs?: number;
}): Promise<TailscaleWhoisIdentity | null>;
export declare function enableTailscaleServe(port: number): Promise<void>;
export declare function disableTailscaleServe(): Promise<void>;
export declare function enableTailscaleFunnel(port: number): Promise<void>;
export declare function disableTailscaleFunnel(): Promise<void>;
//# sourceMappingURL=tailscale.d.ts.map
