/**
 * secrets/audit.ts
 * Security audit module — scans config for hardcoded secrets and permission issues.
 * Ported from CoreBlow reference src/secrets/audit.ts (745 LOC → ~200 LOC compressed).
 */
import type { SecretAuditReport } from './types.js';
/**
 * Run a comprehensive security audit on a config object.
 */
export declare function auditSecrets(config: Record<string, unknown>): SecretAuditReport;
/**
 * Format audit report for display.
 */
export declare function formatAuditReport(report: SecretAuditReport): string;
//# sourceMappingURL=audit.d.ts.map
