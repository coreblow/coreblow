/**
 * Plugin Base Types
 *
 * Foundation types shared across all plugin subsystems.
 * Following CoreBlow's types.ts pattern but split into focused modules
 * per CoreBlow's feature-per-file architecture.
 */
/** Branded plugin identifier — reverse-domain preferred (e.g., "@coreblow/plugin-weather") */
export type PluginId = string & {
    readonly __brand: 'PluginId';
};
/** Create a PluginId from a raw string */
export declare function toPluginId(raw: string): PluginId;
/** Logger passed into plugin registration, services, and CLI surfaces. */
export type PluginLogger = {
    debug?: (message: string) => void;
    info: (message: string) => void;
    warn: (message: string) => void;
    error: (message: string) => void;
};
/** Plugin format — how the plugin is structured */
export type PluginFormat = 'coreblow' | 'coreblow' | 'npm' | 'local';
/** Plugin bundle format — how the plugin is packaged */
export type PluginBundleFormat = 'esm' | 'cjs' | 'ts' | 'bundle';
/** Plugin origin — where the plugin was discovered */
export type PluginOrigin = 'bundled' | 'workspace' | 'global' | 'config' | 'npm';
/** Plugin status */
export type PluginStatus = 'loaded' | 'disabled' | 'error';
/** Plugin registration mode */
export type PluginRegistrationMode = 'full' | 'setup-only';
/** Plugin kind (for specialized plugins) */
export type PluginKind = 'memory' | 'context-engine' | 'general';
/** Config UI hints for dashboard/forms */
export type PluginConfigUiHint = {
    label?: string;
    help?: string;
    tags?: string[];
    advanced?: boolean;
    sensitive?: boolean;
    placeholder?: string;
};
/** Config validation result */
export type PluginConfigValidation = {
    ok: true;
    value?: unknown;
} | {
    ok: false;
    errors: string[];
};
/**
 * Config schema contract accepted by plugin manifests and runtime registration.
 * Plugins can provide a Zod-like parser, a lightweight validate() function, or both.
 */
export type PluginConfigSchema = {
    safeParse?: (value: unknown) => {
        success: boolean;
        data?: unknown;
        error?: {
            issues?: Array<{
                path: Array<string | number>;
                message: string;
            }>;
        };
    };
    parse?: (value: unknown) => unknown;
    validate?: (value: unknown) => PluginConfigValidation;
    uiHints?: Record<string, PluginConfigUiHint>;
    jsonSchema?: Record<string, unknown>;
};
/** Plugin diagnostic — warnings and errors from loading/registration */
export type PluginDiagnostic = {
    level: 'info' | 'warn' | 'error';
    pluginId: string;
    source: string;
    message: string;
};
/**
 * Plugin record — the registry's view of a loaded plugin.
 * Mirrors CoreBlow's PluginRecord type.
 */
export type PluginRecord = {
    id: string;
    name: string;
    version?: string;
    description?: string;
    format?: PluginFormat;
    bundleFormat?: PluginBundleFormat;
    bundleCapabilities?: string[];
    kind?: PluginKind;
    source: string;
    rootDir?: string;
    origin: PluginOrigin;
    workspaceDir?: string;
    enabled: boolean;
    status: PluginStatus;
    error?: string;
    toolNames: string[];
    hookNames: string[];
    channelIds: string[];
    providerIds: string[];
    speechProviderIds: string[];
    imageGenerationProviderIds: string[];
    webSearchProviderIds: string[];
    gatewayMethods: string[];
    cliCommands: string[];
    services: string[];
    commands: string[];
    httpRoutes: number;
    hookCount: number;
    configSchema: boolean;
    configUiHints?: Record<string, PluginConfigUiHint>;
    configJsonSchema?: Record<string, unknown>;
};
/** Create an empty PluginRecord */
export declare function createPluginRecord(params: {
    id: string;
    name?: string;
    description?: string;
    version?: string;
    format?: PluginFormat;
    bundleFormat?: PluginBundleFormat;
    bundleCapabilities?: string[];
    source: string;
    rootDir?: string;
    origin: PluginOrigin;
    workspaceDir?: string;
    enabled: boolean;
    configSchema?: boolean;
}): PluginRecord;
//# sourceMappingURL=types.base.d.ts.map
