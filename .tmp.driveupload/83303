/**
 * plugin-sdk/compat.ts
 * [ADAPT] openclaw-main/src/plugin-sdk/compat.ts
 *
 * Legacy compat surface for external plugins that still depend on older
 * broad plugin-sdk imports. Keep this file intentionally small.
 *
 * Adaptasi: OPENCLAW_ env vars → COREBLOW_, docs URL → coreblow.dev
 */
const shouldWarnCompatImport = process.env.VITEST !== 'true' &&
    process.env.NODE_ENV !== 'test' &&
    process.env.COREBLOW_SUPPRESS_PLUGIN_SDK_COMPAT_WARNING !== '1';
if (shouldWarnCompatImport) {
    process.emitWarning('coreblow/plugin-sdk/compat is deprecated for new plugins. Migrate to focused coreblow/plugin-sdk/<subpath> imports. See https://docs.coreblow.dev/plugins/sdk-migration', {
        code: 'COREBLOW_PLUGIN_SDK_COMPAT_DEPRECATED',
        detail: 'Bundled plugins must use scoped plugin-sdk subpaths. External plugins may keep compat temporarily while migrating. Migration guide: https://docs.coreblow.dev/plugins/sdk-migration',
    });
}
// Re-export commonly needed surfaces for backward compat
export { emptyPluginConfigSchema, resolvePluginConfigSchema, definePluginEntry } from './plugin-entry.js';
export { createPluginRuntimeStore } from './runtime-store.js';
export { KeyedAsyncQueue, enqueueKeyedTask } from './keyed-async-queue.js';
export { createDeferred } from './extension-shared.js';
export { DEFAULT_ACCOUNT_ID, normalizeAccountId } from './account-id.js';
//# sourceMappingURL=compat.js.map
