export class ComponentCollector {
    collected = [];
    options;
    ended = false;
    constructor(options) {
        this.options = options;
    }
    collect(interaction) {
        if (this.ended)
            return false;
        if (this.options.filter && !this.options.filter(interaction))
            return false;
        this.collected.push(interaction);
        if (this.options.maxCollect && this.collected.length >= this.options.maxCollect)
            this.end();
        return true;
    }
    end() { this.ended = true; }
    getCollected() { return [...this.collected]; }
    get isEnded() { return this.ended; }
    get count() { return this.collected.length; }
}
//# sourceMappingURL=collectors.js.map
