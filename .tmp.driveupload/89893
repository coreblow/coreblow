/**
 * process/exec.ts
 * Safe command execution with timeout and output capture.
 * Ported from CoreBlow reference src/process/exec.ts.
 */
export interface ExecResult {
    stdout: string;
    stderr: string;
    exitCode: number;
    killed: boolean;
    durationMs: number;
}
export interface ExecOptions {
    cwd?: string;
    env?: NodeJS.ProcessEnv;
    timeoutMs?: number;
    maxBuffer?: number;
    shell?: boolean;
    stdin?: string;
}
/**
 * Execute a command and capture output.
 */
export declare function exec(command: string, args?: string[], opts?: ExecOptions): Promise<ExecResult>;
/**
 * Execute a shell command string.
 */
export declare function execShell(command: string, opts?: ExecOptions): Promise<ExecResult>;
/**
 * Check if a command exists.
 */
export declare function commandExists(command: string): Promise<boolean>;
//# sourceMappingURL=exec.d.ts.map
