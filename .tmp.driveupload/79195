/**
 * cli/config/cli-config-store.ts
 */
import fs from 'node:fs';
export class CLIConfigStore {
    path;
    constructor(p) { this.path = p; }
    save(config) { fs.writeFileSync(this.path, JSON.stringify(config, null, 2)); }
    load() { try {
        return JSON.parse(fs.readFileSync(this.path, 'utf-8'));
    }
    catch {
        return {};
    } }
}
//# sourceMappingURL=cli-config-store.js.map
