/**
 * flows/model-picker.ts — Interactive model selection flow.
 */
import type { FlowDefinition } from './types.js';
export interface ModelCandidate {
    id: string;
    provider: string;
    contextWindow: number;
    costPer1k: number;
    speed: 'fast' | 'medium' | 'slow';
    capabilities: string[];
}
export declare function rankModels(candidates: ModelCandidate[], criteria: {
    priority: 'cost' | 'speed' | 'context' | 'capabilities';
    requiredCaps?: string[];
}): ModelCandidate[];
export declare function formatModelComparison(models: ModelCandidate[]): string;
/**
 * Creates an interactive model picker flow.
 */
export declare function createModelPickerFlow(availableModels: ModelCandidate[], onCompleteAction?: (data: Record<string, unknown>) => Promise<void> | void): FlowDefinition;
//# sourceMappingURL=model-picker.d.ts.map
