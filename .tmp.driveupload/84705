/**
 * plugin-sdk/provider-zai-endpoint.ts
 * [ADAPT] openclaw-main/src/plugin-sdk/provider-zai-endpoint.ts
 *
 * Z.AI endpoint detection helpers for provider plugins.
 * Adaptasi: CoreBlow stub — Z.AI is OpenClaw-specific service.
 */
/** Detect Z.AI / CoreBlow endpoint from env or config. */
export function detectZaiEndpoint(params) {
    const baseUrl = params?.baseUrl ?? (params?.envVar ? process.env[params.envVar] : undefined);
    if (!baseUrl)
        return null;
    return {
        endpointId: 'custom',
        baseUrl: baseUrl.replace(/\/$/, ''),
        label: 'Custom endpoint',
    };
}
//# sourceMappingURL=provider-zai-endpoint.js.map
