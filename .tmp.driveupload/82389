export function resolveGatewayCredentialsFromConfig(options) {
    const cfg = options.cfg ?? options.config;
    const auth = cfg?.gateway?.auth;
    const authRecord = auth;
    const mode = options.modeOverride || authRecord?.mode || "none";
    if (mode === "none")
        return {};
    return {
        token: authRecord?.token || process.env.GATEWAY_TOKEN,
        password: authRecord?.password || process.env.GATEWAY_PASSWORD
    };
}
//# sourceMappingURL=credentials.js.map
