/**
 * agents/fast-mode.ts
 * Fast mode — skip expensive operations for speed.
 */
let fastMode = false;
export function enableFastMode() { fastMode = true; }
export function disableFastMode() { fastMode = false; }
export function isFastMode() { return fastMode; }
export function skipIfFastMode(fn, fallback) { return fastMode ? fallback : fn(); }
export async function skipIfFastModeAsync(fn, fallback) { return fastMode ? fallback : fn(); }
//# sourceMappingURL=fast-mode.js.map
