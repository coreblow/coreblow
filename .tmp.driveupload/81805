/**
 * CoreBlow — Tenant Manager
 *
 * Manages multi-tenant environments with tenant
 * registration, suspension, metadata, and isolation.
 */
/** Tenant */
export interface Tenant {
    id: string;
    name: string;
    slug: string;
    status: 'active' | 'suspended' | 'trial' | 'deactivated';
    plan: string;
    createdAt: number;
    metadata?: Record<string, unknown>;
    limits: {
        maxUsers: number;
        maxChannels: number;
        maxAgents: number;
    };
    userCount: number;
}
/**
 * CoreBlow Tenant Manager
 */
export declare class TenantManager {
    private tenants;
    private idCounter;
    /**
     * Create a tenant.
     */
    create(name: string, plan?: string, limits?: Partial<Tenant['limits']>): Tenant;
    /**
     * Get a tenant.
     */
    get(id: string): Tenant | null;
    /**
     * Find by slug.
     */
    findBySlug(slug: string): Tenant | null;
    /**
     * Suspend a tenant.
     */
    suspend(id: string, reason?: string): boolean;
    /**
     * Reactivate a tenant.
     */
    activate(id: string): boolean;
    /**
     * Update plan.
     */
    updatePlan(id: string, plan: string, limits?: Partial<Tenant['limits']>): boolean;
    /**
     * List tenants.
     */
    list(status?: Tenant['status']): Array<{
        id: string;
        name: string;
        status: string;
        plan: string;
    }>;
    /**
     * Delete a tenant.
     */
    delete(id: string): boolean;
    /** Count */
    count(): number;
}
//# sourceMappingURL=tenant-manager.d.ts.map
