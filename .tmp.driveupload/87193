export function resolveRegistryState(activeCount, isShuttingDown) { if (isShuttingDown)
    return activeCount > 0 ? 'draining' : 'shutdown'; return activeCount > 0 ? 'active' : 'empty'; }
//# sourceMappingURL=subagent-registry-state.js.map
