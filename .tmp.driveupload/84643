/**
 * plugin-sdk/account-helpers.ts
 * [PORT] openclaw-main/src/plugin-sdk/account-helpers.ts
 *
 * Account list helpers for multi-account channel plugins.
 */
import { DEFAULT_ACCOUNT_ID, normalizeAccountId } from './account-id.js';
export { DEFAULT_ACCOUNT_ID, normalizeAccountId };
export type AccountEntry<TAccount> = {
    accountId: string;
    account: TAccount;
};
export type AccountListHelpers<TAccount> = {
    /** List all configured account IDs. */
    listAccountIds: () => string[];
    /** Resolve an account by ID, throwing if not found. */
    requireAccount: (accountId: string) => TAccount;
    /** Resolve an account by ID, returning undefined if not found. */
    getAccount: (accountId: string) => TAccount | undefined;
    /** List all account entries. */
    listAccounts: () => AccountEntry<TAccount>[];
};
/**
 * Build a set of account-list accessor helpers scoped to a record of accounts.
 * [PORT] openclaw createAccountListHelpers
 */
export declare function createAccountListHelpers<TAccount>(accounts: Record<string, TAccount>): AccountListHelpers<TAccount>;
/** Describe an account snapshot in human-readable form. */
export declare function describeAccountSnapshot(snapshot: {
    accountId?: string | null;
    label?: string | null;
}): string;
/** Merge base account config with account-specific overrides. */
export declare function mergeAccountConfig<TConfig extends object>(base: Partial<TConfig>, override: Partial<TConfig>): TConfig;
/** Resolve merged account config with deep merge for nested objects. */
export declare function resolveMergedAccountConfig<TConfig extends Record<string, unknown>>(base: Partial<TConfig>, override: Partial<TConfig>): TConfig;
/** Resolve the first configured account ID that matches the default. */
export declare function resolveListedDefaultAccountId(accountIds: string[], defaultId?: string): string;
//# sourceMappingURL=account-helpers.d.ts.map
