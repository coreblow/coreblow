/**
 * agents/sandbox-paths.ts
 * Sandbox path utilities.
 */
import path from 'node:path';
export function normalizeSandboxPath(p, baseDir) { return path.resolve(baseDir, p); }
export function isPathTraversal(p) { return p.includes('..') || p.includes('\0'); }
export function stripPathPrefix(fullPath, baseDir) { const rel = path.relative(baseDir, fullPath); return rel.startsWith('..') ? fullPath : rel; }
export function isHiddenPath(p) { return path.basename(p).startsWith('.'); }
export function ensureWithinBase(targetPath, baseDir) {
    const resolved = path.resolve(baseDir, targetPath);
    return { valid: resolved.startsWith(path.resolve(baseDir)), resolved };
}
//# sourceMappingURL=sandbox-paths.js.map
