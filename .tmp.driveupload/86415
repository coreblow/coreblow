/**
 * CoreBlow — Blue-Green Deployer
 *
 * Manages blue-green deployment strategy with traffic
 * switching, canary percentages, and instant rollback.
 */
/** Slot */
export interface DeploySlot {
    name: 'blue' | 'green';
    version: string;
    status: 'active' | 'standby' | 'deploying';
    deployedAt?: number;
    healthCheck?: () => Promise<boolean>;
}
/**
 * CoreBlow Blue-Green Deployer
 */
export declare class BlueGreenDeployer {
    private blue;
    private green;
    private activeSlot;
    private canaryPercent;
    private history;
    /**
     * Deploy to standby slot.
     */
    deploy(version: string): Promise<{
        success: boolean;
        slot: string;
    }>;
    /**
     * Switch traffic to standby.
     */
    switchTraffic(): {
        success: boolean;
        activeSlot: string;
    };
    /**
     * Set canary percentage.
     */
    setCanary(percent: number): void;
    /**
     * Route request (simulate).
     */
    routeRequest(): string;
    /**
     * Rollback to previous.
     */
    rollback(): boolean;
    /**
     * Get status.
     */
    getStatus(): {
        activeSlot: string;
        blue: DeploySlot;
        green: DeploySlot;
        canaryPercent: number;
    };
    /**
     * Set health check for slot.
     */
    setHealthCheck(slot: 'blue' | 'green', fn: () => Promise<boolean>): void;
    /**
     * Get history.
     */
    getHistory(): typeof this.history;
}
//# sourceMappingURL=blue-green-deployer.d.ts.map
