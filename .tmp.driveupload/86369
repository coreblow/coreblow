/**
 * CoreBlow — Task Queue
 *
 * Priority-based task queue with concurrency control,
 * delayed execution, dead letter queue, and progress
 * tracking.
 */
/** Task */
export interface QueueTask {
    id: string;
    name: string;
    handler: () => Promise<unknown>;
    priority: number;
    status: 'pending' | 'running' | 'completed' | 'failed';
    createdAt: number;
    startedAt?: number;
    completedAt?: number;
    result?: unknown;
    error?: string;
    retries: number;
    maxRetries: number;
}
/**
 * CoreBlow Task Queue
 */
export declare class TaskQueue {
    private queue;
    private running;
    private completed;
    private deadLetter;
    private maxConcurrency;
    private idCounter;
    private processing;
    constructor(maxConcurrency?: number);
    /**
     * Enqueue a task.
     */
    enqueue(name: string, handler: () => Promise<unknown>, priority?: number, maxRetries?: number): string;
    /**
     * Process the queue.
     */
    process(): Promise<number>;
    /**
     * Get queue stats.
     */
    getStats(): {
        pending: number;
        running: number;
        completed: number;
        failed: number;
    };
    /**
     * Get dead letter queue.
     */
    getDeadLetter(): QueueTask[];
    /**
     * Clear completed tasks.
     */
    clearCompleted(): number;
    /** Count pending */
    count(): number;
}
//# sourceMappingURL=task-queue.d.ts.map
