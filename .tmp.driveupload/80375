/**
 * Discord Thread Manager — Manages thread lifecycle and metadata.
 */
export interface ThreadInfo {
    id: string;
    name: string;
    parentId: string;
    createdAt: number;
    archived: boolean;
    locked: boolean;
    messageCount: number;
}
export declare class ThreadManager {
    private threads;
    track(id: string, name: string, parentId: string): void;
    archive(id: string): boolean;
    unarchive(id: string): boolean;
    lock(id: string): boolean;
    incrementMessages(id: string): void;
    get(id: string): ThreadInfo | undefined;
    listByParent(parentId: string): ThreadInfo[];
    listActive(): ThreadInfo[];
    get count(): number;
}
//# sourceMappingURL=manager.d.ts.map
