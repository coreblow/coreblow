/**
 * plugin-sdk/provider-model-shared.ts
 * [PORT] openclaw-main/src/plugin-sdk/provider-model-shared.ts
 *
 * Shared model definition types for provider plugins.
 */
export type { ModelDefinition } from './provider-catalog-shared.js';
/** Shared cost structure for model pricing. */
export type ModelCost = {
    inputPer1kTokens?: number;
    outputPer1kTokens?: number;
    cachedInputPer1kTokens?: number;
};
/** Model capability flags. */
export type ModelCapabilities = {
    vision?: boolean;
    tools?: boolean;
    reasoning?: boolean;
    streaming?: boolean;
    json?: boolean;
    audio?: boolean;
    images?: boolean;
};
/** Full model definition for provider catalogs. */
export type ProviderModelDefinition = {
    id: string;
    label?: string;
    contextLength?: number;
    maxOutputTokens?: number;
    cost?: ModelCost;
    capabilities?: ModelCapabilities;
    isDefault?: boolean;
    aliases?: string[];
    [key: string]: unknown;
};
/** Build a standard model definition with defaults. */
export declare function buildModelDefinition(params: ProviderModelDefinition): ProviderModelDefinition;
/** Normalize a model ID to lowercase slug. */
export declare function normalizeModelId(id: string): string;
/** Check if a model ID matches by id or alias. */
export declare function matchesModelId(model: ProviderModelDefinition, id: string): boolean;
//# sourceMappingURL=provider-model-shared.d.ts.map
