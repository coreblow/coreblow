/**
 * plugin-sdk/channel-status.ts
 * [ADAPT] openclaw-main/src/plugin-sdk/channel-status.ts
 *
 * Channel account status snapshot helpers.
 */
export type { ChannelAccountSnapshot, ChannelStatusIssue } from './channel-contract.js';
export declare const PAIRING_APPROVED_MESSAGE = "Pairing approved.";
/** Credential status for a field in an account snapshot. */
export type CredentialStatus = 'ok' | 'missing' | 'invalid' | 'expired';
export type CredentialSnapshot = {
    field: string;
    status: CredentialStatus;
    message?: string;
};
/** Project credential snapshot fields from a raw config object. */
export declare function projectCredentialSnapshotFields(config: Record<string, unknown>, fields: string[]): CredentialSnapshot[];
/** Resolve configured status from an array of credential snapshots. */
export declare function resolveConfiguredFromCredentialStatuses(statuses: CredentialSnapshot[]): boolean;
/** Resolve configured status from required credential fields only. */
export declare function resolveConfiguredFromRequiredCredentialStatuses(statuses: CredentialSnapshot[], requiredFields: string[]): boolean;
/** Build a computed account status snapshot. */
export declare function buildComputedAccountStatusSnapshot(params: {
    accountId: string;
    configured: boolean;
    running?: boolean;
    error?: string | null;
    extra?: Record<string, unknown>;
}): {
    accountId: string;
    configured: boolean;
    running: boolean;
    error: string | null;
};
/** Build a token-based channel status summary. */
export declare function buildTokenChannelStatusSummary(params: {
    accountId: string;
    token?: string | null;
    running?: boolean;
    error?: string | null;
}): {
    accountId: string;
    configured: boolean;
    running: boolean;
    error: string | null;
};
//# sourceMappingURL=channel-status.d.ts.map
