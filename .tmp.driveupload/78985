/**
 * commands/sessions.ts
 *
 * `coreblow sessions` — list active gateway sessions.
 * [PORT] openclaw-main/src/commands/sessions.ts (adapted)
 */
import type { Runtime } from '../runtime.js';
export type SessionsCommandOptions = {
    json?: boolean;
    store?: string;
    agent?: string;
    allAgents?: boolean;
    active?: string;
};
export declare function sessionsCommand(opts: SessionsCommandOptions, runtime: Runtime): Promise<void>;
//# sourceMappingURL=sessions.d.ts.map
