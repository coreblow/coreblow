/**
 * CoreBlow AutoPilot — normalizeInboundText
 *
 * Normalize inbound text for processing.
 */
export function normalizeInboundText(text) {
    return text
        .replace(/\r\n/g, '\n')
        .replace(/\r/g, '\n')
        .replace(/\u200B/g, '') // zero-width space
        .trim();
}
//# sourceMappingURL=normalize-inbound-text.js.map
