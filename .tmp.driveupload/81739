/**
 * gateway/protocol/schema.ts
 * JSON-RPC protocol schema definitions for the gateway.
 */
export const RPC_ERROR_CODES = {
    PARSE_ERROR: -32700,
    INVALID_REQUEST: -32600,
    METHOD_NOT_FOUND: -32601,
    INVALID_PARAMS: -32602,
    INTERNAL_ERROR: -32603,
    UNAUTHORIZED: -32001,
    FORBIDDEN: -32003,
};
export function createRPCResponse(id, result) {
    return { jsonrpc: '2.0', result, id };
}
export function createRPCError(id, code, message) {
    return { jsonrpc: '2.0', error: { code, message }, id };
}
export function isValidRPCRequest(data) {
    if (typeof data !== 'object' || data === null)
        return false;
    const obj = data;
    return obj.jsonrpc === '2.0' && typeof obj.method === 'string';
}
//# sourceMappingURL=schema.js.map
