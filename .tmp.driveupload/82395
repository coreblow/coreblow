import { createEndpoint } from "./http-endpoint-helpers.js";
import { sendJsonResponse } from "./http-utils.js";
export const sessionsHistoryEndpoint = createEndpoint("GET", "/api/sessions/", (req, res) => {
    // Expected path: /api/sessions/:id/history
    if (req.url?.endsWith("/history")) {
        sendJsonResponse(res, 200, { history: [] });
    }
    else {
        sendJsonResponse(res, 404, { error: "invalid session endpoint" });
    }
});
//# sourceMappingURL=sessions-history-http.js.map
