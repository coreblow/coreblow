export interface DockerExecResult {
    stdout: string;
    stderr: string;
    exitCode: number;
}
export declare class DockerRuntime {
    private running;
    private containerId?;
    private image;
    private name;
    constructor(image?: string, name?: string);
    /**
     * Check apakah Docker CLI tersedia di PATH.
     * Pola defensif — panggil sebelum instantiate DockerRuntime.
     *
     * @example
     * if (await DockerRuntime.isAvailable()) {
     *   const runtime = new DockerRuntime();
     *   await runtime.start();
     * }
     */
    static isAvailable(): Promise<boolean>;
    start(): Promise<boolean>;
    stop(): Promise<void>;
    isRunning(): boolean;
    exec(cmd: string, timeout?: number): Promise<DockerExecResult>;
    getContainerId(): string | undefined;
    private run;
}
//# sourceMappingURL=docker-runtime.d.ts.map
