/**
 * CoreBlow Health Check Endpoints
 *
 * Comprehensive health check system with liveness, readiness, and
 * dependency checks. Supports custom probes and status aggregation.
 *
 * Equivalent: CoreBlow gateway health patterns (~350 LOC)
 */
export type HealthStatus = 'healthy' | 'degraded' | 'unhealthy';
export interface HealthCheckResult {
    status: HealthStatus;
    uptime: number;
    timestamp: number;
    version: string;
    checks: Record<string, ComponentHealth>;
}
export interface ComponentHealth {
    status: HealthStatus;
    message?: string;
    latencyMs?: number;
    lastChecked: number;
    metadata?: Record<string, unknown>;
}
export type HealthProbe = () => Promise<ComponentHealth>;
export declare function setVersion(version: string): void;
export declare function registerProbe(name: string, probe: HealthProbe): void;
export declare function removeProbe(name: string): boolean;
export declare function clearProbes(): void;
export declare function checkHealth(): Promise<HealthCheckResult>;
export declare function checkLiveness(): Promise<{
    alive: boolean;
}>;
export declare function checkReadiness(): Promise<{
    ready: boolean;
    reason?: string;
}>;
export declare function createMemoryProbe(maxHeapMB?: number): HealthProbe;
export declare function createUptimeProbe(minUptimeMs?: number): HealthProbe;
export declare function createEventLoopProbe(maxDelayMs?: number): HealthProbe;
//# sourceMappingURL=health-check.d.ts.map
