/**
 * cron/history.ts
 */
export class CronHistory {
    entries = [];
    record(jobId, durationMs, success, error) { this.entries.push({ jobId, ranAt: Date.now(), durationMs, success, error }); if (this.entries.length > 1000)
        this.entries.shift(); }
    getByJob(id) { return this.entries.filter(e => e.jobId === id); }
    getFailures() { return this.entries.filter(e => !e.success); }
}
//# sourceMappingURL=history.js.map
