/**
 * plugin-sdk/provider-setup.ts
 * [ADAPT] openclaw-main/src/plugin-sdk/provider-setup.ts
 *
 * Provider setup helpers: configure self-hosted and API-key providers.
 */
import type { CoreBlowConfig } from '../config/config.js';
export type { CoreBlowConfig };
export type { ProviderAuthResult } from './provider-auth-result.js';
export type { ProviderModelDefinition } from './provider-model-shared.js';
export declare const SELF_HOSTED_DEFAULT_CONTEXT_WINDOW = 8192;
export declare const SELF_HOSTED_DEFAULT_COST: {
    inputPer1kTokens: number;
    outputPer1kTokens: number;
};
export declare const SELF_HOSTED_DEFAULT_MAX_TOKENS = 4096;
export type DiscoveredModel = {
    id: string;
    label?: string;
    contextLength?: number;
};
/** Discover OpenAI-compatible models from a self-hosted base URL. */
export declare function discoverOpenAICompatibleLocalModels(params: {
    baseUrl: string;
    apiKey?: string;
    timeoutMs?: number;
}): Promise<DiscoveredModel[]>;
/** Discover models from a self-hosted OpenAI-compatible provider. */
export declare function discoverOpenAICompatibleSelfHostedProvider(params: {
    baseUrl: string;
    apiKey?: string;
}): Promise<DiscoveredModel[]>;
/** Configure a self-hosted provider non-interactively. */
export declare function configureOpenAICompatibleSelfHostedProviderNonInteractive(params: {
    cfg: CoreBlowConfig;
    providerId: string;
    baseUrl: string;
    apiKey?: string;
    defaultModelId?: string;
}): Promise<CoreBlowConfig>;
/** Prompt and configure a self-hosted provider. */
export declare function promptAndConfigureOpenAICompatibleSelfHostedProvider(params: {
    cfg: CoreBlowConfig;
    providerId: string;
    prompt: (msg: string) => Promise<string>;
}): Promise<CoreBlowConfig>;
/** Prompt and configure auth for a self-hosted provider. */
export declare function promptAndConfigureOpenAICompatibleSelfHostedProviderAuth(params: {
    cfg: CoreBlowConfig;
    providerId: string;
    prompt: (msg: string) => Promise<string>;
}): Promise<CoreBlowConfig>;
/** Stub vllm provider builder. */
export declare function buildVllmProvider(params: {
    baseUrl?: string;
}): Record<string, unknown>;
/** Stub sglang provider builder. */
export declare function buildSglangProvider(params: {
    baseUrl?: string;
}): Record<string, unknown>;
//# sourceMappingURL=provider-setup.d.ts.map
