import AppIntents

struct SetThemeIntent: AppIntent {
    static var title: LocalizedStringResource = "SetTheme"
    static var description = IntentDescription("CoreBlow SetTheme")

    func perform() async throws -> some IntentResult {
        return .result()
    }
}
