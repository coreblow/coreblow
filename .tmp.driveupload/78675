/**
 * sessions/session-manager.ts
 */
export class SessionManager {
    sessions = new Map();
    create(id, model) { this.sessions.set(id, { model, messages: [], createdAt: Date.now() }); return id; }
    get(id) { return this.sessions.get(id); }
    addMessage(id, msg) { this.sessions.get(id)?.messages.push(msg); }
    delete(id) { this.sessions.delete(id); }
    list() { return [...this.sessions.keys()]; }
    count() { return this.sessions.size; }
}
//# sourceMappingURL=session-manager.js.map
