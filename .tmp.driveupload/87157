/** Subagent announcement output formatting. */
export { formatAnnouncement } from './subagent-announce.js';
export function formatAnnouncementList(announcements) { return announcements.map((a) => `[${a.agentId}] ${a.status}: ${a.task}`).join('\n'); }
//# sourceMappingURL=subagent-announce-output.js.map
