/**
 * agents/agent-paths.ts
 * Agent directory and file path resolution.
 * Ported from CoreBlow reference src/agents/agent-paths.ts.
 */
export interface AgentPaths {
    agentDir: string;
    sessionsDir: string;
    workspaceDir: string;
    configPath: string;
    contextDir: string;
    sessionPath: (sessionId: string) => string;
    transcriptPath: (sessionId: string) => string;
}
/**
 * Resolve the CoreBlow agent directory from a base path.
 */
export declare function resolveAgentDir(basePath: string, agentId?: string): string;
/**
 * Resolve all agent paths from an agent directory.
 */
export declare function resolveAgentPaths(agentDir: string): AgentPaths;
/**
 * Ensure all agent directories exist.
 */
export declare function ensureAgentDirs(agentDir: string): void;
/**
 * List all session IDs for an agent.
 */
export declare function listAgentSessions(agentDir: string): string[];
/**
 * Resolve the default agent workspace directory.
 */
export declare function resolveDefaultWorkspaceDir(env?: NodeJS.ProcessEnv): string;
/**
 * Strip null bytes from paths (prevent ENOTDIR on malformed input).
 */
export declare function sanitizePath(p: string): string;
//# sourceMappingURL=agent-paths.d.ts.map
