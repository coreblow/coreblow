/**
 * commands/batch.ts
 */
export class BatchExecutor {
    results = [];
    async executeBatch(commands, executor) { this.results = []; for (const cmd of commands) {
        const result = await executor(cmd);
        this.results.push({ command: cmd, result });
    } return this.results; }
}
//# sourceMappingURL=batch.js.map
