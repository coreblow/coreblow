/**
 * CoreBlow — Rollback Manager
 *
 * Manages rollback points and automated rollback
 * for deployments, configurations, and database
 * migrations.
 */
/** Rollback point */
export interface RollbackPoint {
    id: string;
    type: 'deployment' | 'config' | 'migration' | 'custom';
    description: string;
    state: Record<string, unknown>;
    createdAt: number;
    size: number;
}
/**
 * CoreBlow Rollback Manager
 */
export declare class RollbackManager {
    private points;
    private idCounter;
    private maxPoints;
    private history;
    /**
     * Create a rollback point.
     */
    create(type: RollbackPoint['type'], description: string, state: Record<string, unknown>): RollbackPoint;
    /**
     * Rollback to a point.
     */
    rollback(pointId: string, reason?: string): {
        success: boolean;
        state?: Record<string, unknown>;
    };
    /**
     * Rollback to latest of a type.
     */
    rollbackLatest(type: RollbackPoint['type'], reason?: string): {
        success: boolean;
        state?: Record<string, unknown>;
    };
    /**
     * Get a point.
     */
    get(id: string): RollbackPoint | null;
    /**
     * List by type.
     */
    listByType(type: RollbackPoint['type']): RollbackPoint[];
    /**
     * Get rollback history.
     */
    getHistory(): typeof this.history;
    /**
     * Get total storage size.
     */
    getTotalSize(): number;
    /** Count */
    count(): number;
}
//# sourceMappingURL=rollback-manager.d.ts.map
