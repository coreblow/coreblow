/**
 * sessions/rate-limit.ts
 */
export class SessionRateLimit {
    counts = new Map();
    check(sessionId, max = 100, windowMs = 60000) { const now = Date.now(); const entry = this.counts.get(sessionId); if (!entry || now > entry.resetAt) {
        this.counts.set(sessionId, { count: 1, resetAt: now + windowMs });
        return true;
    } entry.count++; return entry.count <= max; }
}
//# sourceMappingURL=rate-limit.js.map
