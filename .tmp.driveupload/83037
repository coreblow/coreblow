/**
 * auto-reply/reply/normalize-reply.ts
 * Output normalization — strip thinking tags, format per channel.
 */
/** Strip <thinking>...</thinking> tags from AI output. */
export declare function stripThinkingTags(content: string): string;
/** Strip assistant prefixes like "Assistant:" or "Bot:". */
export declare function stripAssistantPrefix(content: string): string;
/** Normalize markdown for platforms that don't support it. */
export declare function stripMarkdown(content: string): string;
/** Format reply content for a specific platform. */
export declare function normalizeReply(content: string, platform: string): string;
/** Truncate reply if it exceeds max length. */
export declare function truncateReply(content: string, maxLength: number): string;
//# sourceMappingURL=normalize-reply.d.ts.map
