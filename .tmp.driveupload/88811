/**
 * CoreBlow Tool Display Utilities
 *
 * Common utilities for formatting tool call outputs, progress indicators,
 * diff displays, file previews, and structured result rendering.
 *
 * Equivalent: CoreBlow src/agents/tool-display-common.ts (452 LOC)
 */
export interface ToolDisplayOptions {
    maxOutputLength?: number;
    truncateMiddle?: boolean;
    showTimestamp?: boolean;
    colorize?: boolean;
    indentLevel?: number;
}
export interface FilePreview {
    path: string;
    language: string;
    content: string;
    startLine?: number;
    endLine?: number;
    totalLines: number;
    truncated: boolean;
}
export interface DiffBlock {
    path: string;
    hunks: DiffHunk[];
    additions: number;
    deletions: number;
}
export interface DiffHunk {
    oldStart: number;
    oldCount: number;
    newStart: number;
    newCount: number;
    lines: DiffLine[];
}
export interface DiffLine {
    type: 'add' | 'remove' | 'context';
    content: string;
    oldLineNo?: number;
    newLineNo?: number;
}
export interface ProgressIndicator {
    label: string;
    current: number;
    total: number;
    startedAt: number;
    status: 'running' | 'complete' | 'error';
}
/**
 * Truncate output to max length with ellipsis
 */
export declare function truncateOutput(output: string, maxLength?: number, mode?: 'end' | 'middle'): {
    text: string;
    truncated: boolean;
    originalLength: number;
};
/**
 * Format a tool result for display
 */
export declare function formatToolResult(params: {
    toolName: string;
    output: string;
    error?: string;
    durationMs: number;
    exitCode?: number;
    options?: ToolDisplayOptions;
}): string;
/**
 * Format a file preview with line numbers
 */
export declare function formatFilePreview(preview: FilePreview): string;
/**
 * Format a diff block
 */
export declare function formatDiff(diff: DiffBlock): string;
/**
 * Format a progress indicator
 */
export declare function formatProgress(indicator: ProgressIndicator): string;
/**
 * Render a text-based progress bar
 */
export declare function renderProgressBar(percent: number, width?: number): string;
/**
 * Format a duration in milliseconds to human-readable
 */
export declare function formatDuration(ms: number): string;
/**
 * Format a file size in bytes to human-readable
 */
export declare function formatFileSize(bytes: number): string;
/**
 * Infer the language of output based on tool name and content
 */
export declare function inferLanguage(toolName: string, content: string): string;
/**
 * Get the language for a file path
 */
export declare function getFileLanguage(filePath: string): string;
/**
 * Format data as a markdown table
 */
export declare function formatTable(headers: string[], rows: string[][]): string;
/**
 * Format a key-value list
 */
export declare function formatKeyValue(pairs: Array<[string, string]>): string;
//# sourceMappingURL=tool-display-common.d.ts.map
