/**
 * plugin-sdk/provider-model-shared.ts
 * [PORT] openclaw-main/src/plugin-sdk/provider-model-shared.ts
 *
 * Shared model definition types for provider plugins.
 */
/** Build a standard model definition with defaults. */
export function buildModelDefinition(params) {
    return { ...params };
}
/** Normalize a model ID to lowercase slug. */
export function normalizeModelId(id) {
    return id.trim().toLowerCase();
}
/** Check if a model ID matches by id or alias. */
export function matchesModelId(model, id) {
    const normalized = normalizeModelId(id);
    if (normalizeModelId(model.id) === normalized)
        return true;
    return (model.aliases ?? []).some((a) => normalizeModelId(a) === normalized);
}
//# sourceMappingURL=provider-model-shared.js.map
