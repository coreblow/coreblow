type EnsureCoreBlowPathOpts = {
    execPath?: string;
    cwd?: string;
    homeDir?: string;
    platform?: NodeJS.Platform;
    pathEnv?: string;
    allowProjectLocalBin?: boolean;
};
/**
 * Best-effort PATH bootstrap so skills that require the `coreblow` CLI can run
 * under launchd/minimal environments (and inside the macOS app bundle).
 */
export declare function ensureCoreBlowCliOnPath(opts?: EnsureCoreBlowPathOpts): void;
export {};
//# sourceMappingURL=path-env.d.ts.map
