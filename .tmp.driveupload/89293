/**
 * CoreBlow — Skill System
 *
 * Reusable, composable skill modules that agents can learn
 * and use. Each skill bundles a prompt, tools, and examples
 * into a self-contained capability.
 */
/** Skill definition */
export interface Skill {
    id: string;
    name: string;
    description: string;
    category: string;
    /** System prompt addition */
    systemPrompt?: string;
    /** Associated tool names */
    tools?: string[];
    /** Few-shot examples */
    examples?: Array<{
        input: string;
        output: string;
    }>;
    /** Required skills */
    requires?: string[];
    /** Activation keywords */
    triggers?: string[];
    enabled: boolean;
    version: string;
}
/** Skill match result */
export interface SkillMatch {
    skill: Skill;
    confidence: number;
    trigger?: string;
}
/**
 * CoreBlow Skill System
 */
export declare class SkillSystem {
    private skills;
    constructor();
    /**
     * Register a skill.
     */
    register(skill: Skill): void;
    /**
     * Get a skill by ID.
     */
    get(id: string): Skill | null;
    /**
     * Match skills based on user input.
     */
    match(input: string, topK?: number): SkillMatch[];
    /**
     * Build system prompt additions for active skills.
     */
    buildPromptAdditions(skillIds: string[]): string;
    /**
     * Enable/disable a skill.
     */
    setEnabled(id: string, enabled: boolean): boolean;
    /**
     * List skills by category.
     */
    listByCategory(): Record<string, Array<{
        id: string;
        name: string;
        enabled: boolean;
    }>>;
    /**
     * List all skills.
     */
    list(): Array<{
        id: string;
        name: string;
        category: string;
        enabled: boolean;
    }>;
    /** Count */
    count(): number;
}
//# sourceMappingURL=skill-system.d.ts.map
