import { RichResponse } from '../components/rich-response.js';
import { PROVIDER_COLORS } from '../constants.js';
export function modelCommand(_channel) {
    return {
        name: 'model',
        description: 'View or switch the AI model',
        options: [{ name: 'name', description: 'Model name to switch to', type: 3, required: false }],
        handler: async (interaction) => {
            const name = interaction.options?.getString?.('name');
            if (name) {
                return new RichResponse()
                    .quickEmbed(' Model Changed', `Switched to **${name}**`, 0x10A37F)
                    .buttons([{ label: 'Test it', customId: 'test_model', style: 'primary', emoji: '' }]);
            }
            return new RichResponse()
                .quickEmbed(' AI Model', 'Select a model from the dropdown below', PROVIDER_COLORS.default)
                .selectMenu({
                customId: 'model_select', placeholder: 'Choose a model...',
                options: [
                    { label: 'GPT-4o', value: 'gpt-4o', description: 'OpenAI flagship', emoji: '' },
                    { label: 'Claude 3.5 Sonnet', value: 'claude-3.5-sonnet', description: 'Anthropic', emoji: '' },
                    { label: 'Gemini Pro', value: 'gemini-pro', description: 'Google', emoji: '' },
                    { label: 'Llama 3', value: 'llama3', description: 'Local via Ollama', emoji: '' },
                ],
            });
        },
    };
}
//# sourceMappingURL=model.js.map
