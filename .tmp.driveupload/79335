/**
 * src/media/understanding.ts
 * Media Understanding — image, audio, video, OCR, document parsing
 * SUPERIOR: CoreBlow only does image+audio; CoreBlow adds video frames, OCR, PDF/DOCX
 */
export interface MediaAnalysis {
    type: 'image' | 'audio' | 'video' | 'document';
    description: string;
    metadata: Record<string, unknown>;
    text?: string;
    duration?: number;
    frames?: string[];
}
export interface MediaUnderstandingConfig {
    visionProvider: 'openai' | 'gemini' | 'ollama';
    whisperProvider: 'openai' | 'groq' | 'local';
    openaiKey?: string;
    geminiKey?: string;
    groqKey?: string;
    ollamaUrl?: string;
}
export declare class MediaUnderstanding {
    private config;
    constructor(config?: Partial<MediaUnderstandingConfig>);
    /**
     * Auto-detect media type and analyze
     */
    analyze(url: string, mimeType?: string): Promise<MediaAnalysis>;
    /**
     * Analyze image using vision model
     */
    analyzeImage(url: string): Promise<MediaAnalysis>;
    private analyzeImageGemini;
    /**
     * Transcribe audio using Whisper
     */
    transcribeAudio(url: string): Promise<MediaAnalysis>;
    /**
     * Analyze video — extract key frames and describe each
     * SUPERIOR: CoreBlow doesn't have video analysis
     */
    analyzeVideo(url: string): Promise<MediaAnalysis>;
    /**
     * Parse documents — PDF, DOCX, XLSX text extraction
     * SUPERIOR: CoreBlow doesn't have document parsing
     */
    parseDocument(url: string): Promise<MediaAnalysis>;
    /**
     * Detect media type from URL or MIME type
     */
    private detectType;
    private urlToBase64;
}
//# sourceMappingURL=understanding.d.ts.map
