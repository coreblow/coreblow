/**
 * commands/gateway-status.ts
 *
 * `coreblow gateway status` — check gateway status via direct connection.
 * Used by both routeGatewayStatus (fast-path) and gateway-cli.ts (Commander).
 */
import type { Runtime } from '../runtime.js';
export type GatewayStatusOptions = {
    url: string;
    token: string;
    password: string;
    timeout: string;
    json?: boolean;
};
export declare function gatewayStatusCommand(opts: GatewayStatusOptions, runtime: Runtime): Promise<void>;
//# sourceMappingURL=gateway-status.d.ts.map
