/** Live model filtering. */
export function filterModels(models, query) {
    let result = models.filter((m) => !m.disabled);
    if (query)
        result = result.filter((m) => m.id.toLowerCase().includes(query.toLowerCase()));
    return result;
}
//# sourceMappingURL=live-model-filter.js.map
