/**
 * Discord Config Resolution
 */
import type { DiscordConfig } from './types.js';
export interface ResolvedConfig extends DiscordConfig {
    token: string;
    prefix: string;
    allowedChannels: string[];
    richResponses: boolean;
    threadMode: 'off' | 'auto' | 'manual';
}
export declare function resolveConfig(config: DiscordConfig): ResolvedConfig;
export declare function validateConfig(config: unknown): config is DiscordConfig;
//# sourceMappingURL=config.d.ts.map
