/** OpenAI WS message format conversion. */
export function convertWsToContentBlock(msg) {
    const type = msg.type;
    if (type === 'response.text.delta')
        return { type: 'text', content: msg.delta };
    if (type === 'response.done')
        return { type: 'done' };
    return { type: 'unknown' };
}
//# sourceMappingURL=openai-ws-message-conversion.js.map
