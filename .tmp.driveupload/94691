import SwiftUI

struct ModelPickerView: View {
    var body: some View {
        NavigationStack {
            VStack {
                Text("ModelPickerView")
            }
            .navigationTitle("ModelPicker")
        }
    }
}
