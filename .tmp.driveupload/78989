/**
 * commands/batch.ts
 */
export declare class BatchExecutor {
    private results;
    executeBatch(commands: string[], executor: (cmd: string) => Promise<string>): Promise<Array<{
        command: string;
        result: string;
    }>>;
}
//# sourceMappingURL=batch.d.ts.map
