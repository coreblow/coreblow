/**
 * plugin-sdk/slack-target-parser.ts
 * [ADAPT] openclaw-main — Stub for @coreblow/slack.
 */
export function parseSlackTarget(raw) {
    const match = raw.match(/^(#?[a-z0-9_-]+|C[A-Z0-9]{8,}|U[A-Z0-9]{8,}|<#.+>|<@.+>)$/i);
    return match ? { kind: "channel", id: raw.replace(/[<>#@]/g, "") } : null;
}
export function resolveSlackChannelId(raw) {
    return raw.replace(/[<>#@]/g, "").split("|")[0] ?? raw;
}
//# sourceMappingURL=slack-target-parser.js.map
