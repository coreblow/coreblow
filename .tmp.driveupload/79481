/**
 * tts/streaming.ts
 */
export class TTSStreamer {
    chunks = [];
    addChunk(chunk) { this.chunks.push(chunk); }
    flush() { return Buffer.concat(this.chunks); }
    clear() { this.chunks = []; }
}
//# sourceMappingURL=streaming.js.map
