/**
 * gateway/server-wizard-sessions.ts — In-memory wizard session tracker
 */
// In CoreBlow, we just store session tracking in simple maps
// For a complete implementation, we'd integrate with FlowEngine or state models.
export function createWizardSessionTracker() {
    const wizardSessions = new Map();
    return {
        wizardSessions,
        findRunningWizard() {
            // Check if there is already a running wizard
            for (const [id, session] of wizardSessions.entries()) {
                if (session.getStatus?.() === 'running') {
                    return id;
                }
            }
            return null;
        },
        purgeWizardSession(id) {
            wizardSessions.delete(id);
        }
    };
}
//# sourceMappingURL=server-wizard-sessions.js.map
