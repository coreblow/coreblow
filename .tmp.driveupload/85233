/**
 * src/shared/node-list-parse.ts
 * Type definitions and parsing for node lists.
 * Ported from CoreBlow shared/node-list-types.ts and shared/node-list-parse.ts.
 */
// ── Parsing ──
function asRecord(value) {
    return typeof value === "object" && value !== null ? value : {};
}
export function parsePairingList(value) {
    const obj = asRecord(value);
    const pending = Array.isArray(obj.pending) ? obj.pending : [];
    const paired = Array.isArray(obj.paired) ? obj.paired : [];
    return { pending, paired };
}
export function parseNodeList(value) {
    const obj = asRecord(value);
    return Array.isArray(obj.nodes) ? obj.nodes : [];
}
export function formatNodeListEntry(entry) {
    return entry.displayName || entry.remoteIp || entry.nodeId;
}
//# sourceMappingURL=node-list-parse.js.map
