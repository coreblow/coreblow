/**
 * pairing/pairing-store.ts
 * Persistent pairing request storage with file-lock safety.
 * Ported from CoreBlow reference src/pairing/pairing-store.ts.
 */
export type PairingRequest = {
    id: string;
    code: string;
    createdAt: string;
    lastSeenAt: string;
    meta?: Record<string, string>;
};
/**
 * Generate a human-readable pairing code (no ambiguous chars).
 */
export declare function generatePairingCode(length?: number): string;
export declare class PairingStore {
    private storeDir;
    constructor(storeDir: string);
    private get storePath();
    private get allowFromPath();
    /**
     * Upsert a pairing request. Returns existing code if within TTL, otherwise creates new.
     */
    upsert(params: {
        id: string;
        meta?: Record<string, string>;
    }): Promise<{
        code: string;
        created: boolean;
    }>;
    /**
     * Accept a pairing request by code.
     */
    accept(code: string): Promise<PairingRequest | null>;
    /**
     * Reject a pairing request by code.
     */
    reject(code: string): Promise<PairingRequest | null>;
    /**
     * Check if a sender is in the allow-from list.
     */
    isAllowed(senderId: string): boolean;
    /**
     * List pending pairing requests.
     */
    listPending(): PairingRequest[];
    /**
     * List allowed sender IDs.
     */
    listAllowed(): string[];
    /**
     * Revoke a paired sender.
     */
    revoke(senderId: string): boolean;
    private readStore;
    private writeStore;
    private readAllowFrom;
    private writeAllowFrom;
}
//# sourceMappingURL=pairing-store.d.ts.map
