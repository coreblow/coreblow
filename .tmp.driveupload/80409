/**
 * src/channels/discord/channel.ts
 * Main DiscordChannel class — implements ChannelAdapter
 * Uses modular components from the 82-file architecture
 */
import type { ChannelAdapter, ChannelStatus } from '../interface.js';
import type { MessageRouter } from '../../gateway/router.js';
import type { DiscordConfig, SlashCommand, DiscordModal } from './types.js';
import type { DiscordInteraction } from './types-sdk.js';
export declare class DiscordChannel implements ChannelAdapter {
    name: string;
    private client;
    private config;
    private connected;
    private startedAt;
    private router?;
    private slashCommands;
    private stats;
    private paginationState;
    constructor(config: DiscordConfig);
    registerSlashCommand(command: SlashCommand): void;
    getAllSlashCommands(): SlashCommand[];
    registerBuiltinCommands(): void;
    start(router: MessageRouter): Promise<void>;
    private sendMessage;
    showModal(interaction: DiscordInteraction, modal: DiscordModal): Promise<void>;
    stop(): Promise<void>;
    isConnected(): boolean;
    getStatus(): ChannelStatus;
}
//# sourceMappingURL=channel.d.ts.map
