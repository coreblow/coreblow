/**
 * routing/message-router.ts
 */
export class MessageRouter {
    routes = [];
    add(pattern, handler) { this.routes.push({ pattern: new RegExp(pattern, 'i'), handler }); }
    async route(text, ctx) { for (const r of this.routes) {
        if (r.pattern.test(text))
            return r.handler(text, ctx);
    } return null; }
}
//# sourceMappingURL=message-router.js.map
