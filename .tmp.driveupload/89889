/**
 * process/pool.ts
 */
export declare class ProcessPool {
    private workers;
    private maxSize;
    constructor(maxSize?: number);
    acquire(): unknown;
    release(worker: unknown): void;
    size(): number;
}
//# sourceMappingURL=pool.d.ts.map
