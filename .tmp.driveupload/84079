/**
 * plugin-sdk/account-id.ts
 * [PORT] openclaw-main/src/plugin-sdk/account-id.ts
 *
 * Account ID normalization utilities.
 */
/** Default account ID string used when no specific account is configured. */
export const DEFAULT_ACCOUNT_ID = 'default';
/** Normalize a raw account ID string, falling back to DEFAULT_ACCOUNT_ID. */
export function normalizeAccountId(accountId) {
    const trimmed = accountId?.trim();
    return trimmed || DEFAULT_ACCOUNT_ID;
}
/** Normalize an optional account ID, returning undefined if empty/null. */
export function normalizeOptionalAccountId(accountId) {
    const trimmed = accountId?.trim();
    return trimmed || undefined;
}
//# sourceMappingURL=account-id.js.map
