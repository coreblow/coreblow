/**
 * CoreBlow — Lazy Loader
 *
 * Lazy initialization of expensive resources with
 * caching, TTL, and dependency-aware loading.
 */
/**
 * CoreBlow Lazy Loader
 */
export declare class LazyLoader {
    private resources;
    private stats;
    /**
     * Register a lazy resource.
     */
    register<T>(name: string, factory: () => Promise<T>, options?: {
        ttlMs?: number;
        deps?: string[];
    }): void;
    /**
     * Get a resource (loads on first access).
     */
    get<T>(name: string): Promise<T | null>;
    /**
     * Check if loaded.
     */
    isLoaded(name: string): boolean;
    /**
     * Invalidate (force reload next access).
     */
    invalidate(name: string): boolean;
    /**
     * Get stats.
     */
    getStats(): typeof this.stats;
    /**
     * List resources.
     */
    list(): Array<{
        name: string;
        loaded: boolean;
        deps: string[];
    }>;
    /** Count */
    count(): number;
}
//# sourceMappingURL=lazy-loader.d.ts.map
