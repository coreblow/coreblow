/**
 * agents/sanitize-for-prompt.ts
 * Sanitization utilities for safe embedding in LLM prompts.
 * Ported from CoreBlow reference src/agents/sanitize-for-prompt.ts + console-sanitize.ts.
 */
/**
 * Sanitize text for safe embedding in system prompts.
 * Strips potential injection markers and control characters.
 */
export declare function sanitizeForPromptLiteral(text: string): string;
/**
 * Strip ANSI escape codes from console output.
 */
export declare function stripAnsi(text: string): string;
/**
 * Truncate text to max length with ellipsis.
 */
export declare function truncateForPrompt(text: string, maxLen: number): string;
/**
 * Sanitize console output for embedding in tool results.
 */
export declare function sanitizeConsoleOutput(output: string, opts?: {
    maxLines?: number;
    maxChars?: number;
    stripAnsi?: boolean;
}): string;
/**
 * Escape markdown special characters.
 */
export declare function escapeMarkdown(text: string): string;
/**
 * Strip markdown formatting to plain text.
 */
export declare function stripMarkdown(text: string): string;
/**
 * Detect potential prompt injection attempts.
 */
export declare function detectPromptInjection(text: string): {
    detected: boolean;
    patterns: string[];
};
//# sourceMappingURL=sanitize-for-prompt.d.ts.map
