/**
 * sessions/session-cleanup.ts
 */
export function cleanupSessions(sessions, maxAgeMs = 3600000) { const now = Date.now(); let cleaned = 0; for (const [id, s] of sessions) {
    if (now - s.createdAt > maxAgeMs) {
        sessions.delete(id);
        cleaned++;
    }
} return cleaned; }
//# sourceMappingURL=session-cleanup.js.map
