/**
 * process/manager.ts
 */
export class ProcessManager {
    procs = new Map();
    register(name, pid) { this.procs.set(name, { pid, status: 'running' }); }
    stop(name) { const p = this.procs.get(name); if (p) {
        try {
            process.kill(p.pid, 'SIGTERM');
        }
        catch { /* intentionally ignored */ }
        p.status = 'stopped';
    } }
    getAll() { return Object.fromEntries(this.procs); }
}
//# sourceMappingURL=manager.js.map
