/**
 * agents/tool-display.ts
 * Format tool calls/results for display.
 */
export function formatToolCall(name, args) {
    const argStr = Object.entries(args).map(([k, v]) => `${k}=${typeof v === 'string' ? `"${v.slice(0, 100)}"` : JSON.stringify(v)}`).join(', ');
    return `${name}(${argStr})`;
}
export function formatToolResult(name, result, maxLen = 200) {
    const truncated = result.length > maxLen ? result.slice(0, maxLen) + `... (${result.length} chars)` : result;
    return `← ${name}: ${truncated}`;
}
export function formatToolCallBrief(name) { return `🔧 ${name}`; }
export function formatToolError(name, error) { return `❌ ${name}: ${error}`; }
export function formatToolCallId(id) { return id.length > 12 ? id.slice(0, 12) + '…' : id; }
//# sourceMappingURL=tool-display.js.map
