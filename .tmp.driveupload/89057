/**
 * CoreBlow — Auth Profile Doctor (CoreBlow Parity)
 *
 * Diagnostic tool to scan and report auth profile health.
 */
import type { AuthProfileStore } from './types.js';
export interface DiagnosticEntry {
    profileId: string;
    provider: string;
    type: string;
    eligible: boolean;
    reasonCode: string;
    inCooldown: boolean;
    cooldownRemainingMs: number;
    issues: string[];
}
export interface DiagnosticReport {
    totalProfiles: number;
    eligibleProfiles: number;
    inCooldownProfiles: number;
    providers: string[];
    entries: DiagnosticEntry[];
    globalIssues: string[];
}
export declare function diagnoseAuthProfiles(store: AuthProfileStore, now?: number): DiagnosticReport;
//# sourceMappingURL=doctor.d.ts.map
