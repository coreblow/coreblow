/**
 * secrets/env-secrets.ts
 * Environment variable secret management with allowlists and redaction.
 * Extended from CoreBlow placeholder to match CoreBlow patterns.
 */
/**
 * Get a secret from environment variables.
 */
export declare function getSecret(name: string): string | undefined;
/**
 * Get a required secret — throws if missing.
 */
export declare function requireSecret(name: string): string;
/**
 * Get a secret with a fallback default.
 */
export declare function getSecretOrDefault(name: string, fallback: string): string;
/**
 * Check if a secret is configured (non-empty).
 */
export declare function hasSecret(name: string): boolean;
/**
 * Scan environment for all secrets matching sensitive prefixes.
 * Returns variable names (not values) for audit purposes.
 */
export declare function scanSensitiveEnvVars(prefixes?: string[]): string[];
/**
 * Scan environment for secrets matching an allowlist.
 * Only returns values for allowed keys.
 */
export declare function resolveEnvSecrets(keys: string[], opts?: {
    allowlist?: string[];
    required?: boolean;
}): Map<string, string>;
/**
 * Create a redacted copy of process.env for safe logging.
 */
export declare function redactEnv(env?: NodeJS.ProcessEnv, prefixes?: string[]): Record<string, string>;
//# sourceMappingURL=env-secrets.d.ts.map
