import { queues } from './queue.data.js';
export function dequeue(sessionKey, policy) {
    const queue = queues.get(sessionKey);
    if (!queue || queue.length === 0)
        return undefined;
    if (policy === 'latest') {
        const item = queue.pop();
        queues.set(sessionKey, []);
        return item;
    }
    return queue.shift();
}
//# sourceMappingURL=dequeue.js.map
