/**
 * extensions/public-artifacts.ts
 * Public artifact serving extension.
 * Ported from CoreBlow reference src/extensions/public-artifacts.ts.
 */
export interface PublicArtifact {
    id: string;
    filename: string;
    mimeType: string;
    sizeBytes: number;
    createdAt: number;
    expiresAt?: number;
    metadata?: Record<string, unknown>;
}
export declare class PublicArtifactStore {
    private artifactsDir;
    private registry;
    constructor(artifactsDir: string);
    /**
     * Store a new artifact and return its public ID.
     */
    store(params: {
        filename: string;
        content: Buffer;
        mimeType: string;
        ttlMs?: number;
        metadata?: Record<string, unknown>;
    }): PublicArtifact;
    /**
     * Retrieve artifact metadata by ID.
     */
    get(id: string): PublicArtifact | null;
    /**
     * Read artifact content.
     */
    readContent(id: string): Buffer | null;
    /**
     * Delete an artifact.
     */
    delete(id: string): boolean;
    /**
     * List all artifacts.
     */
    list(): PublicArtifact[];
    /**
     * Prune expired artifacts.
     */
    pruneExpired(): number;
    /**
     * Total storage used.
     */
    totalSizeBytes(): number;
}
//# sourceMappingURL=public-artifacts.d.ts.map
