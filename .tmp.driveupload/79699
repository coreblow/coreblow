export function recursiveChunk(text, config = {}) { const max = config.maxChunkSize || 1000; const overlap = config.overlap || 100; const chunks = []; let i = 0, idx = 0; while (i < text.length) {
    chunks.push({ content: text.slice(i, i + max), index: idx++ });
    i += max - overlap;
} return chunks; }
//# sourceMappingURL=recursive-chunker.js.map
