/**
 * media-understanding/document-parser.ts
 */
export function parseTextDocument(content) { const lines = content.split('\n'); const title = lines[0] || 'Untitled'; const sections = []; let currentHeading = ''; let currentContent = []; for (const line of lines.slice(1)) {
    if (line.startsWith('#')) {
        if (currentHeading)
            sections.push({ heading: currentHeading, content: currentContent.join('\n') });
        currentHeading = line.replace(/^#+\s*/, '');
        currentContent = [];
    }
    else
        currentContent.push(line);
} if (currentHeading)
    sections.push({ heading: currentHeading, content: currentContent.join('\n') }); return { title, sections }; }
//# sourceMappingURL=document-parser.js.map
