export function createSpan(name, traceId) { return { traceId: traceId ?? `tr_${Date.now().toString(36)}`, spanId: `sp_${Date.now().toString(36)}_${Math.random().toString(36).slice(2, 6)}`, name, startTime: Date.now() }; }
export function endSpan(span) { span.endTime = Date.now(); return span; }
export function spanDurationMs(span) { return (span.endTime ?? Date.now()) - span.startTime; }
//# sourceMappingURL=trace-base.js.map
