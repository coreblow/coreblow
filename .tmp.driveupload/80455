/**
 * src/channels/discord/handlers/command.ts
 * Slash command dispatch and error handling
 */
import type { SlashCommand, ChannelStats } from '../types.js';
import type { DiscordInteraction, PaginationState } from '../types-sdk.js';
/**
 * Handle a slash command interaction
 */
export declare function handleCommand(interaction: DiscordInteraction & {
    deferred?: boolean;
    channel?: {
        id: string;
    };
    user: {
        id: string;
    };
}, commands: Map<string, SlashCommand>, stats: ChannelStats, richResponses: boolean, paginationState: Map<string, PaginationState>): Promise<void>;
//# sourceMappingURL=command.d.ts.map
