/**
 * flows/doctor-health.ts — Health check flow and contribution framework.
 */
import type { FlowDefinition } from './types.js';
import { ContributionRegistry } from './contributions.js';
export interface HealthContribution {
    component: string;
    status: 'healthy' | 'degraded' | 'unhealthy';
    message: string;
    latencyMs?: number;
}
export declare const healthContributionRegistry: ContributionRegistry;
export declare function aggregateHealth(contributions: HealthContribution[]): {
    overall: 'healthy' | 'degraded' | 'unhealthy';
    components: HealthContribution[];
};
export declare function formatHealthReport(contributions: HealthContribution[]): string;
/**
 * Executes a health check gathering data from all registered contributions.
 */
export declare function runHealthCheck(): Promise<string>;
/**
 * Creates a diagnostic flow for interactive health checking.
 */
export declare function createDoctorFlow(onCompleteAction?: (data: Record<string, unknown>) => Promise<void> | void): FlowDefinition;
//# sourceMappingURL=doctor-health.d.ts.map
