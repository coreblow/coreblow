/**
 * image-generation/model-ref.ts
 * Parse "provider/model" format image generation model references.
 * Follows CoreBlow's model-ref.ts exactly.
 */
export function parseImageGenerationModelRef(raw) {
    const trimmed = raw?.trim();
    if (!trimmed)
        return null;
    const slashIndex = trimmed.indexOf('/');
    if (slashIndex <= 0 || slashIndex === trimmed.length - 1)
        return null;
    return {
        provider: trimmed.slice(0, slashIndex).trim(),
        model: trimmed.slice(slashIndex + 1).trim(),
    };
}
//# sourceMappingURL=model-ref.js.map
