/** Skill installation output formatting. */
export function formatDownloadProgress(downloaded: number, total: number): string { const pct = total > 0 ? Math.round((downloaded / total) * 100) : 0; return `Downloading: ${pct}%`; }
