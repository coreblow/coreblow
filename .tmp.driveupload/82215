/**
 * CoreBlow — API Gateway
 *
 * Unified API gateway combining route matching,
 * middleware chain, request pipeline, and response
 * building into a single request handler.
 */
import { RouteMatcher } from './route-matcher.js';
import { type MiddlewareContext, type MiddlewareFn } from './middleware-chain.js';
/** Route handler */
export type RouteHandler = (ctx: MiddlewareContext) => Promise<void>;
/**
 * CoreBlow API Gateway
 */
export declare class ApiGateway {
    private router;
    private middleware;
    private handlers;
    private stats;
    constructor();
    /**
     * Register a route with handler.
     */
    route(method: string, path: string, handler: RouteHandler, mw?: string[]): void;
    /**
     * Convenience methods.
     */
    get(path: string, handler: RouteHandler): void;
    post(path: string, handler: RouteHandler): void;
    /**
     * Add global middleware.
     */
    use(name: string, fn: MiddlewareFn, path?: string): void;
    /**
     * Handle a request.
     */
    handle(method: string, path: string, headers?: Record<string, string>, body?: unknown): Promise<{
        status: number;
        body: unknown;
        headers: Record<string, string>;
    }>;
    /**
     * Get stats.
     */
    getStats(): typeof this.stats;
    /**
     * Get router.
     */
    getRouter(): RouteMatcher;
    /** Count routes */
    count(): number;
}
//# sourceMappingURL=api-gateway.d.ts.map
