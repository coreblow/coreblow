/**
 * CoreBlow — Doc Site Generator
 *
 * Generates documentation site content from module
 * metadata. Supports module docs, API reference,
 * getting started guides, and search indexing.
 */
/** Doc module */
export interface DocModule {
    name: string;
    description: string;
    category: string;
    exports: Array<{
        name: string;
        type: string;
        description: string;
    }>;
    examples?: Array<{
        title: string;
        code: string;
    }>;
}
/** Doc page */
export interface DocPage {
    slug: string;
    title: string;
    content: string;
    category: string;
    order: number;
}
/**
 * CoreBlow Doc Site Generator
 */
export declare class DocSiteGenerator {
    private modules;
    private pages;
    /**
     * Add a module doc.
     */
    addModule(doc: DocModule): void;
    /**
     * Generate module doc page.
     */
    generateModulePage(moduleName: string): string | null;
    /**
     * Generate sidebar navigation.
     */
    generateSidebar(): Array<{
        category: string;
        items: Array<{
            name: string;
            slug: string;
        }>;
    }>;
    /**
     * Generate getting started guide.
     */
    generateGettingStarted(): string;
    /**
     * Build search index.
     */
    buildSearchIndex(): Array<{
        slug: string;
        title: string;
        keywords: string[];
    }>;
    /**
     * Search modules.
     */
    search(query: string): DocModule[];
    /** Count modules */
    count(): number;
    private toSlug;
}
//# sourceMappingURL=doc-site-generator.d.ts.map
