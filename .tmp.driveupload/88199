/**
 * agents/bootstrap-cache.ts
 * Cache management for bootstrap/session data.
 * Ported from CoreBlow reference src/agents/bootstrap-cache.ts.
 */
export interface CacheEntry<T = unknown> {
    key: string;
    value: T;
    createdAt: number;
    expiresAt?: number;
    hitCount: number;
}
export declare class BootstrapCache<T = unknown> {
    private entries;
    private maxSize;
    private defaultTtlMs?;
    constructor(maxSize?: number, defaultTtlMs?: number);
    get(key: string): T | undefined;
    set(key: string, value: T, ttlMs?: number): void;
    has(key: string): boolean;
    delete(key: string): boolean;
    clear(): void;
    size(): number;
    prune(): number;
    keys(): string[];
    values(): T[];
    stats(): {
        size: number;
        maxSize: number;
        totalHits: number;
    };
    private evictOldest;
}
//# sourceMappingURL=bootstrap-cache.d.ts.map
