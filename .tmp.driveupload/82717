/**
 * media-understanding/defaults.ts
 * Default prompts and configuration for media analysis.
 * Ported from CoreBlow reference src/media-understanding/defaults.ts.
 */
export declare const DEFAULT_IMAGE_PROMPT = "Describe this image in detail. Include all visible text, important visual elements, colors, and layout.";
export declare const DEFAULT_AUDIO_PROMPT = "Transcribe this audio. Include timestamps if available.";
export declare const DEFAULT_VIDEO_PROMPT = "Describe the contents of this video, including visual elements, audio, and key events.";
export declare const DEFAULT_DOCUMENT_PROMPT = "Summarize the contents of this document.";
export declare const DEFAULT_IMAGE_MODEL = "gpt-4o";
export declare const DEFAULT_AUDIO_MODEL = "whisper-1";
export declare const DEFAULT_VIDEO_MODEL = "gemini-2.5-pro";
export declare const DEFAULT_MAX_TOKENS = 4096;
export declare const DEFAULT_DETAIL: "auto";
export declare const DEFAULT_SAMPLE_FRAMES = 5;
export declare const DEFAULT_TIMEOUT_MS = 60000;
/**
 * Resolve the analysis prompt for a media type.
 */
export declare function resolveMediaPrompt(params: {
    type: string;
    customPrompt?: string;
    cfg?: Record<string, unknown>;
}): string;
/**
 * Resolve model for media analysis.
 */
export declare function resolveMediaModel(type: string, cfg?: Record<string, unknown>): string;
/**
 * Resolve max tokens for media analysis.
 */
export declare function resolveMediaMaxTokens(modelMaxTokens?: number, requestedMaxTokens?: number): number;
//# sourceMappingURL=defaults.d.ts.map
