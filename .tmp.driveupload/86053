/**
 * CoreBlow — Message Broker
 *
 * In-memory message broker with queues, consumers,
 * acknowledgment, retry, and priority support.
 */
/** Message */
export interface BrokerMessage {
    id: string;
    queue: string;
    payload: unknown;
    priority: number;
    attempts: number;
    maxAttempts: number;
    createdAt: number;
    processedAt?: number;
    status: 'pending' | 'processing' | 'completed' | 'failed';
}
/** Consumer */
export interface Consumer {
    id: string;
    queue: string;
    handler: (msg: BrokerMessage) => Promise<boolean>;
}
/**
 * CoreBlow Message Broker
 */
export declare class MessageBroker {
    private queues;
    private consumers;
    private idCounter;
    private stats;
    /**
     * Publish a message.
     */
    publish(queue: string, payload: unknown, priority?: number, maxAttempts?: number): BrokerMessage;
    /**
     * Subscribe a consumer.
     */
    subscribe(queue: string, handler: Consumer['handler']): string;
    /**
     * Process next message in queue.
     */
    processNext(queue: string): Promise<BrokerMessage | null>;
    /**
     * Get queue depth.
     */
    depth(queue: string): number;
    /**
     * Get stats.
     */
    getStats(): typeof this.stats;
    /**
     * List queues.
     */
    listQueues(): Array<{
        name: string;
        depth: number;
        consumers: number;
    }>;
}
//# sourceMappingURL=message-broker.d.ts.map
