/**
 * gateway/shutdown.ts
 */
export class GracefulShutdown {
    handlers = [];
    shuttingDown = false;
    onShutdown(handler) { this.handlers.push(handler); }
    async shutdown(signal) { if (this.shuttingDown)
        return; this.shuttingDown = true; for (const h of this.handlers) {
        try {
            await h();
        }
        catch { /* intentionally ignored */ }
    } process.exit(0); }
    isShuttingDown() { return this.shuttingDown; }
}
//# sourceMappingURL=shutdown.js.map
