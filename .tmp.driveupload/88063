/**
 * CoreBlow Current Time Helper
 *
 * Provides timezone-aware current time formatting and date utilities.
 *
 * Equivalent: CoreBlow src/agents/current-time.ts (40 LOC)
 */
export type TimeFormat = '12h' | '24h';
export interface FormattedTime {
    iso: string;
    display: string;
    timezone: string;
    timestamp: number;
    dayOfWeek: string;
}
/**
 * Get the current time formatted for display
 */
export declare function getCurrentTime(timezone?: string, format?: TimeFormat): FormattedTime;
/**
 * Format a timestamp for prompt injection
 */
export declare function formatTimeForPrompt(timezone?: string): string;
/**
 * Parse a timezone string, returning undefined if invalid
 */
export declare function validateTimezone(tz: string): string | undefined;
//# sourceMappingURL=current-time.d.ts.map
