/**
 * CoreBlow — Alert Manager
 *
 * Manages alert rules, firing conditions, escalation
 * policies, silencing, and alert history.
 */
/** Alert rule */
export interface AlertRule {
    id: string;
    name: string;
    metric: string;
    condition: 'gt' | 'lt' | 'eq' | 'gte' | 'lte';
    threshold: number;
    severity: 'info' | 'warning' | 'critical';
    cooldownMs: number;
    enabled: boolean;
    lastFired?: number;
}
/** Fired alert */
export interface FiredAlert {
    id: string;
    ruleId: string;
    ruleName: string;
    severity: string;
    value: number;
    threshold: number;
    message: string;
    firedAt: number;
    acknowledged: boolean;
    resolvedAt?: number;
}
/**
 * CoreBlow Alert Manager
 */
export declare class AlertManager {
    private rules;
    private alerts;
    private silenced;
    private idCounter;
    private alertCounter;
    /**
     * Add an alert rule.
     */
    addRule(name: string, metric: string, condition: AlertRule['condition'], threshold: number, severity?: AlertRule['severity'], cooldownMs?: number): string;
    /**
     * Evaluate a metric value against rules.
     */
    evaluate(metric: string, value: number): FiredAlert[];
    /**
     * Acknowledge an alert.
     */
    acknowledge(alertId: string): boolean;
    /**
     * Resolve an alert.
     */
    resolve(alertId: string): boolean;
    /**
     * Silence a rule.
     */
    silence(ruleId: string): void;
    /**
     * Unsilence a rule.
     */
    unsilence(ruleId: string): void;
    /**
     * Get active (unresolved) alerts.
     */
    getActive(): FiredAlert[];
    /**
     * Get alert history.
     */
    getHistory(limit?: number): FiredAlert[];
    /** Count rules */
    countRules(): number;
    private checkCondition;
}
//# sourceMappingURL=alert-manager.d.ts.map
