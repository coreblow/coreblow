/**
 * plugin-sdk/telegram-allow-from.ts
 * [PORT] openclaw-main
 */
export function isNumericTelegramUserId(value) {
    return /^\d+$/.test(value.trim());
}
export function normalizeTelegramAllowFromEntry(raw) {
    const trimmed = raw.trim();
    if (trimmed.startsWith("@"))
        return trimmed.toLowerCase();
    return trimmed;
}
//# sourceMappingURL=telegram-allow-from.js.map
