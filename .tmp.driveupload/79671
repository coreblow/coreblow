/**
 * extensions/compaction.ts
 * Conversation compaction (summary-based context reduction).
 * Ported from CoreBlow reference src/agents/pi-extensions/compaction-*.
 */
export interface CompactionConfig {
    enabled: boolean;
    triggerThresholdTokens: number;
    targetTokens: number;
    preserveRecentMessages: number;
    compactionPrompt: string;
    qualityCheck: boolean;
}
export interface CompactionResult {
    originalTokens: number;
    compactedTokens: number;
    summaryLength: number;
    preservedMessages: number;
    durationMs: number;
}
/**
 * Resolve compaction config.
 */
export declare function resolveCompactionConfig(cfg?: Record<string, unknown>): CompactionConfig;
/**
 * Check if compaction should be triggered.
 */
export declare function shouldCompact(currentTokens: number, config: CompactionConfig): boolean;
/**
 * Build the compaction messages for the LLM.
 */
export declare function buildCompactionMessages(params: {
    messages: Array<{
        role: string;
        content: string;
    }>;
    config: CompactionConfig;
}): {
    toCompact: Array<{
        role: string;
        content: string;
    }>;
    toPreserve: Array<{
        role: string;
        content: string;
    }>;
};
/**
 * Validate compaction quality (summary should not be too short or too long).
 */
export declare function validateCompactionQuality(summary: string, originalTokens: number): {
    valid: boolean;
    reason?: string;
};
//# sourceMappingURL=compaction.d.ts.map
