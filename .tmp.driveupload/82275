/**
 * gateway/server-startup.ts — Core subsystem boot sequence
 */
import { buildGatewayCronService } from "./server-cron.js";
import { createWizardSessionTracker } from "./server-wizard-sessions.js";
// Imports to underlying hook & skills subsystems would go here
// import { HookEngine } from "../hooks/engine.js";
export async function bootstrapCoreSubsystems(config) {
    // 1. Boot Cron
    const cron = buildGatewayCronService(config);
    // 2. Boot Wizard Tracker
    const wizardTracker = createWizardSessionTracker();
    // 3. Boot Hooks (Placeholder for actual hook engine loading)
    // const hooks = new HookEngine();
    // hooks.loadInternalHooks(...);
    // 4. Boot Skills Discovery
    // const skillsRegistry = new SkillsRegistry();
    // skillsRegistry.discover(...);
    return {
        cron,
        ...wizardTracker,
        // hooks,
        // skillsRegistry
    };
}
//# sourceMappingURL=server-startup.js.map
