/**
 * CoreBlow — Example Generator
 *
 * Generates example request/response payloads,
 * code snippets, and cURL commands for API docs.
 */
/** Example config */
export interface ExampleConfig {
    method: string;
    path: string;
    baseUrl: string;
    headers?: Record<string, string>;
    body?: unknown;
    response?: unknown;
}
/**
 * CoreBlow Example Generator
 */
export declare class ExampleGenerator {
    private defaultBaseUrl;
    private defaultHeaders;
    /**
     * Set defaults.
     */
    setDefaults(baseUrl: string, headers?: Record<string, string>): void;
    /**
     * Generate cURL command.
     */
    toCurl(config: Partial<ExampleConfig> & {
        method: string;
        path: string;
    }): string;
    /**
     * Generate fetch snippet.
     */
    toFetch(config: Partial<ExampleConfig> & {
        method: string;
        path: string;
    }): string;
    /**
     * Generate Python requests snippet.
     */
    toPython(config: Partial<ExampleConfig> & {
        method: string;
        path: string;
    }): string;
    /**
     * Generate full example block.
     */
    toMarkdown(config: Partial<ExampleConfig> & {
        method: string;
        path: string;
    }): string;
}
//# sourceMappingURL=example-generator.d.ts.map
