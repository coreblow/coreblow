/**
 * secrets/ref-contract.ts
 * Secret reference validation and provider alias resolution.
 * Ported from CoreBlow reference src/secrets/ref-contract.ts.
 */
import type { SecretRef, SecretRefSource } from './types.js';
export declare const SINGLE_VALUE_FILE_REF_ID = "_value";
/**
 * Unique cache key for a secret ref: `source:provider:id`
 */
export declare function secretRefKey(ref: SecretRef): string;
/**
 * Validate a ref ID for exec providers (stricter than env/file).
 */
export declare function isValidExecSecretRefId(id: string): boolean;
/**
 * Format a validation error message for exec ref IDs.
 */
export declare function formatExecSecretRefIdValidationMessage(id: string): string;
/**
 * Validate a ref ID for any source.
 */
export declare function isValidRefId(id: string, source: SecretRefSource): boolean;
/**
 * Resolve the default provider alias for a given source type.
 * CoreBlow pattern: if no provider config exists, fall back to source-specific defaults.
 */
export declare function resolveDefaultSecretProviderAlias(config: Record<string, unknown>, source: SecretRefSource): string;
/**
 * Parse a secret reference string: `source:provider:id`
 */
export declare function parseSecretRefString(refStr: string): SecretRef | null;
/**
 * Format a secret ref as a displayable string.
 */
export declare function formatSecretRef(ref: SecretRef): string;
//# sourceMappingURL=ref-contract.d.ts.map
