/**
 * CoreBlow — API Key Manager
 *
 * Manages API key lifecycle: creation, validation,
 * rotation, rate limiting, and scope control.
 */
/** API key */
export interface ApiKey {
    id: string;
    key: string;
    name: string;
    owner: string;
    scopes: string[];
    rateLimit: number;
    usage: number;
    active: boolean;
    createdAt: number;
    expiresAt?: number;
    lastUsedAt?: number;
}
/**
 * CoreBlow API Key Manager
 */
export declare class ApiKeyManager {
    private keys;
    private keyIndex;
    private idCounter;
    private stats;
    /**
     * Create an API key.
     */
    create(name: string, owner: string, scopes?: string[], rateLimit?: number, expiresInMs?: number): ApiKey;
    /**
     * Validate an API key.
     */
    validate(key: string, requiredScope?: string): {
        valid: boolean;
        apiKey?: ApiKey;
        error?: string;
    };
    /**
     * Rotate a key (new key, same ID).
     */
    rotate(id: string): ApiKey | null;
    /**
     * Deactivate.
     */
    deactivate(id: string): boolean;
    /**
     * List keys for an owner.
     */
    listByOwner(owner: string): ApiKey[];
    /**
     * Get stats.
     */
    getStats(): typeof this.stats;
    /** Count */
    count(): number;
    private generateKey;
}
//# sourceMappingURL=api-key-manager.d.ts.map
