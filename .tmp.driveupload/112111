import SwiftUI
struct ModelBrowserView: View {
    @State private var isLoading = false
    var body: some View {
        Group {
            if isLoading { ProgressView() }
            else { Text("ModelBrowserView") }
        }
    }
}
