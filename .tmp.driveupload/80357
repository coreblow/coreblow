/**
 * Discord Attachment Processor
 * Processes downloaded attachments — resize images, extract text, validate formats.
 */
export interface ProcessedAttachment {
    filename: string;
    contentType: string;
    originalSize: number;
    processedSize: number;
    textContent?: string;
    metadata: Record<string, unknown>;
}
export declare function processAttachment(filename: string, buffer: Buffer, contentType: string): ProcessedAttachment;
export declare function getFileExtension(filename: string): string;
//# sourceMappingURL=processor.d.ts.map
