/**
 * plugin-sdk/global-singleton.ts
 * [PORT] openclaw-main/src/plugin-sdk/global-singleton.ts
 *
 * Global singleton utilities for shared state across plugin module boundaries.
 */
/** Get or create a global singleton by key. */
export declare function getOrCreateGlobalSingleton<T>(key: string, factory: () => T): T;
/** Check if a global singleton exists. */
export declare function hasGlobalSingleton(key: string): boolean;
/** Clear a global singleton by key (for testing). */
export declare function clearGlobalSingleton(key: string): void;
/** Clear all global singletons (for testing). */
export declare function clearAllGlobalSingletons(): void;
/** A time-bounded dedupe cache for string IDs. */
export type ScopedExpiringIdCache = {
    has: (id: string) => boolean;
    set: (id: string) => void;
    delete: (id: string) => void;
    clear: () => void;
    size: () => number;
};
/** Create a scoped expiring ID cache with a TTL. */
export declare function createScopedExpiringIdCache(ttlMs: number): ScopedExpiringIdCache;
//# sourceMappingURL=global-singleton.d.ts.map
