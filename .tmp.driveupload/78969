/**
 * commands/cooldown.ts
 */
export class CommandCooldown {
    cooldowns = new Map();
    defaultMs;
    constructor(defaultMs = 1000) { this.defaultMs = defaultMs; }
    check(userId, command) { const key = `${userId}:${command}`; const last = this.cooldowns.get(key) || 0; if (Date.now() - last < this.defaultMs)
        return false; this.cooldowns.set(key, Date.now()); return true; }
    remaining(userId, command) { const key = `${userId}:${command}`; const last = this.cooldowns.get(key) || 0; return Math.max(0, this.defaultMs - (Date.now() - last)); }
}
//# sourceMappingURL=cooldown.js.map
