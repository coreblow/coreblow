/**
 * gateway/server/ws-connection/connect-policy.ts
 * Connection limits and flood protection for WebSocket.
 */
interface ConnectPolicyConfig {
    maxConnectionsPerIp: number;
    maxTotalConnections: number;
    connectionRatePerMinute: number;
}
/** Check if a new connection should be allowed. */
export declare function shouldAllowConnection(ip: string, config?: Partial<ConnectPolicyConfig>): {
    allowed: boolean;
    reason?: string;
};
/** Record a new connection. */
export declare function recordConnection(ip: string): void;
/** Record a disconnection. */
export declare function recordDisconnection(ip: string): void;
/** Get current connection stats. */
export declare function getConnectionStats(): {
    total: number;
    byIp: Record<string, number>;
};
export {};
//# sourceMappingURL=connect-policy.d.ts.map
