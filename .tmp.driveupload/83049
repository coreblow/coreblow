/**
 * auto-reply/reply/commands-acp/runtime-options.ts
 * Runtime options for ACP sessions.
 */
export interface ACPRuntimeOptions {
    model?: string;
    maxTokens?: number;
    temperature?: number;
    tools?: string[];
    systemPrompt?: string;
    timeout?: number;
    maxIterations?: number;
    sandbox?: boolean;
}
/** Validate runtime options against agent capabilities. */
export declare function validateRuntimeOptions(opts: ACPRuntimeOptions): {
    valid: boolean;
    errors: string[];
};
/** Merge user options with defaults. */
export declare function mergeWithDefaults(opts: Partial<ACPRuntimeOptions>): ACPRuntimeOptions;
/** Parse runtime options from command args. */
export declare function parseRuntimeOptions(args: string[]): Partial<ACPRuntimeOptions>;
//# sourceMappingURL=runtime-options.d.ts.map
