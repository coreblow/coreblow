export interface ContextWindowGuardConfig {
    contextWindow: number;
    reserveTokens?: number;
    safetyMargin?: number;
    warningThreshold?: number;
}
export declare class ContextWindowGuard {
    private config;
    private currentTokens;
    constructor(config: ContextWindowGuardConfig);
    get effectiveLimit(): number;
    get remaining(): number;
    get usage(): number;
    get isNearLimit(): boolean;
    get isExceeded(): boolean;
    add(tokens: number): void;
    set(tokens: number): void;
    reset(): void;
    canFit(additionalTokens: number): boolean;
    formatStatus(): string;
}
//# sourceMappingURL=context-window-guard.d.ts.map
