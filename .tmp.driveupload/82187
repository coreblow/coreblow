/**
 * gateway/server-methods/validation.ts — Validation helpers for RPC params.
 */
import { type Validator } from '../protocol/index.js';
import type { RespondFn } from './types.js';
export declare function assertValidParams<T>(params: unknown, validator: Validator<T>, methodName: string, respond: RespondFn): params is T;
/**
 * Helper to build custom lightweight object validators manually (no Ajv dependency).
 */
export declare function buildObjectValidator<T>(schema: {
    required?: string[];
    properties?: Record<string, {
        type: string;
        enum?: unknown[];
    }>;
}): Validator<T>;
//# sourceMappingURL=validation.d.ts.map
