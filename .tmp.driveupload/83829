/**
 * plugin-sdk/agent-config-primitives.ts
 * [ADAPT] openclaw-main/src/plugin-sdk/agent-config-primitives.ts
 *
 * Narrow agent-runtime schema primitives without broader config/runtime surfaces.
 * Adaptasi: zod schemas inline karena CoreBlow tidak punya config/zod-schema.core.
 */
import { z } from 'zod';
/** Reply runtime config shape: controls reply strategy per channel. */
export declare const ReplyRuntimeConfigSchemaShape: {
    replyTo: z.ZodOptional<z.ZodEnum<{
        channel: "channel";
        dm: "dm";
        thread: "thread";
    }>>;
    replyWithMention: z.ZodOptional<z.ZodBoolean>;
};
/** Tool policy schema for agent tool use. */
export declare const ToolPolicySchema: z.ZodOptional<z.ZodEnum<{
    always: "always";
    auto: "auto";
    never: "never";
}>>;
/** Inferred reply config type. */
export type ReplyRuntimeConfig = z.infer<z.ZodObject<typeof ReplyRuntimeConfigSchemaShape>>;
/** Inferred tool policy type. */
export type ToolPolicy = z.infer<typeof ToolPolicySchema>;
//# sourceMappingURL=agent-config-primitives.d.ts.map
