export async function handleSessionList(ctx) {
    return '📋 Active sessions:\n  • Current: `' + ctx.sessionId + '` (' + ctx.channel + ')';
}
export async function handleSessionClear(ctx) {
    return '🗑️ Session `' + ctx.sessionId + '` cleared.';
}
export async function handleSessionExport(ctx) {
    return '📦 Session exported. Download at /api/sessions/' + ctx.sessionId + '/export';
}
export async function handleSessionSwitch(ctx) {
    const target = ctx.command.args.id;
    if (!target)
        return 'Usage: /session switch <id>';
    return '✅ Switched to session `' + target + '`.';
}
//# sourceMappingURL=session.js.map
