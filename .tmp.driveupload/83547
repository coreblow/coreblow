/**
 * plugin-sdk/lazy-runtime.ts
 * [PORT] openclaw-main/src/plugin-sdk/lazy-runtime.ts
 *
 * Lazy-initialization runtime helpers for plugins that defer loading
 * of heavy modules or services until first use.
 */
/** A lazy module that loads on first access. */
export type LazyRuntimeModule<T> = {
    get: () => T;
    isLoaded: () => boolean;
    reset: () => void;
};
/** Create a lazy module that initializes on first get(). */
export declare function createLazyRuntimeModule<T>(factory: () => T): LazyRuntimeModule<T>;
/** Create a lazy method that delegates to a lazily-loaded module. */
export declare function createLazyRuntimeMethod<TModule, TMethod extends keyof TModule>(module: LazyRuntimeModule<TModule>, method: TMethod): TModule[TMethod];
/** Create a binder that lazily binds a method from a module. */
export declare function createLazyRuntimeMethodBinder<TModule>(module: LazyRuntimeModule<TModule>): <TMethod extends keyof TModule>(method: TMethod) => TModule[TMethod];
/** Create a lazy named export accessor. */
export declare function createLazyRuntimeNamedExport<T>(factory: () => Promise<T>): () => Promise<T>;
/** Create a lazy surface object where every method delegates lazily. */
export declare function createLazyRuntimeSurface<T extends object>(factory: () => T): T;
//# sourceMappingURL=lazy-runtime.d.ts.map
