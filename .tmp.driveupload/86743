/**
 * CoreBlow — Rate Quota
 *
 * Manages usage quotas per user/organization with
 * daily/monthly limits, usage tracking, and overage
 * handling. Complements token bucket for business-level limits.
 */
/** Quota plan */
export interface QuotaPlan {
    id: string;
    name: string;
    limits: {
        requestsPerDay: number;
        requestsPerMonth: number;
        tokensPerDay: number;
        tokensPerMonth: number;
    };
}
/** Quota usage */
export interface QuotaUsage {
    userId: string;
    planId: string;
    daily: {
        requests: number;
        tokens: number;
        resetAt: number;
    };
    monthly: {
        requests: number;
        tokens: number;
        resetAt: number;
    };
}
/**
 * CoreBlow Rate Quota
 */
export declare class RateQuota {
    private plans;
    private usage;
    constructor();
    /**
     * Add a quota plan.
     */
    addPlan(id: string, name: string, limits: QuotaPlan['limits']): void;
    /**
     * Assign a plan to a user.
     */
    assign(userId: string, planId: string): boolean;
    /**
     * Check if a request is allowed.
     */
    check(userId: string, tokens?: number): {
        allowed: boolean;
        reason?: string;
    };
    /**
     * Record usage.
     */
    record(userId: string, tokens: number): void;
    /**
     * Get usage.
     */
    getUsage(userId: string): QuotaUsage | null;
    /**
     * Get remaining quota.
     */
    getRemaining(userId: string): {
        dailyRequests: number;
        monthlyRequests: number;
        dailyTokens: number;
    } | null;
    /**
     * List plans.
     */
    listPlans(): QuotaPlan[];
    /** Count users */
    count(): number;
    private resetIfExpired;
}
//# sourceMappingURL=rate-quota.d.ts.map
