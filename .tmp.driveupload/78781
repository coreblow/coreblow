export async function handleToolsList(ctx) {
    return '🔧 Available tools:\n  • bash — Execute shell commands\n  • browser — Browse web pages\n  • file_read — Read files\n  • file_write — Write files\n  • search — Search codebase';
}
export async function handleToolsEnable(ctx) {
    const name = ctx.command.args.name;
    if (!name)
        return 'Usage: /tools enable <name>';
    return `✅ Tool \`${name}\` enabled.`;
}
export async function handleToolsDisable(ctx) {
    const name = ctx.command.args.name;
    if (!name)
        return 'Usage: /tools disable <name>';
    return `✅ Tool \`${name}\` disabled.`;
}
export async function handleToolsProfile(ctx) {
    const name = ctx.command.args.name;
    if (!name)
        return 'Usage: /tools profile <name>\nAvailable profiles: default, messaging, coding, full';
    return `✅ Tool profile set to \`${name}\`.`;
}
//# sourceMappingURL=tools.js.map
