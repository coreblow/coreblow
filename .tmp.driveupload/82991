/**
 * auto-reply/reply/reply-dedup.ts
 * Deduplicate identical or near-identical replies.
 * Follows CoreBlow's reply-payloads-dedupe.ts pattern.
 */
export interface DeduplicatedReply {
    content: string;
    isDuplicate: boolean;
    originalHash?: string;
}
/** Check if a reply is a duplicate of a recent reply in the same session. */
export declare function deduplicateReply(sessionId: string, content: string): DeduplicatedReply;
/** Fuzzy dedup — detect near-identical replies (>90% similar). */
export declare function isFuzzyDuplicate(a: string, b: string, threshold?: number): boolean;
/** Clear the dedup cache. */
export declare function clearDedupCache(): void;
//# sourceMappingURL=reply-dedup.d.ts.map
