/**
 * auto-reply/reply/commands-subagents/shared.ts
 * Sub-agent commands in reply context.
 */
export interface SubAgentCommandResult {
    success: boolean;
    message: string;
    data?: unknown;
}
export interface SubAgentOps {
    spawn(name: string, config: {
        model?: string;
        prompt?: string;
        tools?: string[];
    }): Promise<{
        id: string;
        name: string;
    }>;
    list(parentId: string): Array<{
        id: string;
        name: string;
        status: string;
        createdAt: number;
    }>;
    cancel(id: string): boolean;
    cancelAll(parentId: string): number;
    getResult(id: string): {
        content: string;
        status: string;
    } | null;
    sendMessage(id: string, message: string): Promise<string | null>;
}
/** Handle /subagent spawn <name> [--model X] [--prompt "Y"] */
export declare function handleSubAgentSpawn(args: string[], parentSessionId: string, ops: SubAgentOps): Promise<SubAgentCommandResult>;
/** Handle /subagent list */
export declare function handleSubAgentList(parentSessionId: string, ops: SubAgentOps): SubAgentCommandResult;
/** Handle /subagent cancel <id|all> */
export declare function handleSubAgentCancel(args: string[], parentSessionId: string, ops: SubAgentOps): SubAgentCommandResult;
/** Handle /subagent ask <id> <message> */
export declare function handleSubAgentAsk(args: string[], ops: SubAgentOps): Promise<SubAgentCommandResult>;
/** Route sub-agent commands. */
export declare function handleSubAgentCommand(subCommand: string, args: string[], parentSessionId: string, ops: SubAgentOps): Promise<SubAgentCommandResult>;
//# sourceMappingURL=shared.d.ts.map
