import { diagnostics } from './heartbeat.data.js';
export function getDiagnostics(category) {
    if (category)
        return diagnostics.filter(d => d.category === category);
    return [...diagnostics];
}
//# sourceMappingURL=get-diagnostics.js.map
