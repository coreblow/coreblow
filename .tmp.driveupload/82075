export const agentsMutateHandlers = {
    "agents.create": async ({ params, respond }) => {
        respond(true, { id: "agent_mock", scope: "mock_workspace" });
    },
    "agents.update": async ({ params, respond }) => {
        respond(true, { updated: true });
    },
    "agents.delete": async ({ params, respond }) => {
        respond(true, { deleted: true });
    },
    "agents.files.list": async ({ respond }) => respond(true, { files: [] }),
    "agents.files.get": async ({ respond }) => respond(true, { content: "" }),
    "agents.files.set": async ({ respond }) => respond(true, { saved: true })
};
//# sourceMappingURL=agents-mutate.js.map
