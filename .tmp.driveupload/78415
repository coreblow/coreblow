/**
 * config/runtime-schema.ts
 * Runtime config validation layer with deep refinements.
 * Bridges static Zod schema with runtime-specific checks.
 */
export type ValidationSeverity = 'error' | 'warning';
export interface ValidationIssue {
    severity: ValidationSeverity;
    path: string;
    message: string;
    suggestion?: string;
}
/**
 * Validate a config object at runtime with deep refinements.
 * Returns issues (errors block startup, warnings are logged).
 */
export declare function validateRuntimeConfig(cfg: Record<string, unknown>): ValidationIssue[];
/**
 * Check if config has blocking errors.
 */
export declare function hasBlockingErrors(issues: ValidationIssue[]): boolean;
/**
 * Format validation issues for display.
 */
export declare function formatValidationIssues(issues: ValidationIssue[]): string;
//# sourceMappingURL=runtime-schema.d.ts.map
