/**
 * auto-reply/thinking.ts
 * Thinking status and reasoning level management.
 * Ported from CoreBlow reference src/auto-reply/thinking.ts.
 */
export type ThinkLevel = 'none' | 'low' | 'medium' | 'high' | 'max';
export type ReasoningLevel = ThinkLevel;
export type ElevatedLevel = 'standard' | 'elevated';
export type ElevatedMode = 'auto' | 'always' | 'never';
export type VerboseLevel = 'off' | 'brief' | 'full';
export type NoticeLevel = 'off' | 'on';
export type UsageDisplayLevel = 'off' | 'brief' | 'full';
export interface ThinkingCatalogEntry {
    level: ThinkLevel;
    label: string;
    description: string;
    tokenBudget?: number;
}
export declare function normalizeThinkLevel(raw?: string | null): ThinkLevel | undefined;
export declare function normalizeReasoningLevel(raw?: string | null): ReasoningLevel | undefined;
export declare function normalizeElevatedLevel(raw?: string | null): ElevatedLevel | undefined;
export declare function normalizeVerboseLevel(raw?: string | null): VerboseLevel | undefined;
export declare function normalizeUsageDisplay(raw?: string | null): UsageDisplayLevel | undefined;
export declare function normalizeFastMode(raw?: string | null): boolean | undefined;
export declare function listThinkingLevels(): ThinkingCatalogEntry[];
export declare function formatThinkingLevels(): string;
/**
 * Check if a provider only supports binary thinking (on/off vs levels).
 */
export declare function isBinaryThinkingProvider(provider?: string | null): boolean;
/**
 * Resolve the default thinking level for a model.
 */
export declare function resolveThinkingDefaultForModel(model?: string | null): ThinkLevel;
/**
 * Resolve token budget for a thinking level.
 */
export declare function resolveThinkingTokenBudget(level: ThinkLevel): number | undefined;
//# sourceMappingURL=thinking.d.ts.map
