/**
 * agents/turn-engine/config/sessions.ts
 * Session configuration types for the turn engine.
 */
export interface SessionConfig {
    maxIdleMs?: number;
    maxDurationMs?: number;
    maxTurns?: number;
    persistHistory?: boolean;
    historyLimit?: number;
}
export declare const DEFAULT_SESSION_CONFIG: SessionConfig;
export declare function mergeSessionConfig(base: SessionConfig, overrides?: Partial<SessionConfig>): SessionConfig;
//# sourceMappingURL=sessions.d.ts.map
