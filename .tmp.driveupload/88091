const DEFAULT_IDENTITY = { id: 'coreblow', name: 'CoreBlow', persona: 'A helpful AI coding assistant.', version: '1.0.0', capabilities: ['code', 'exec', 'search', 'files'] };
export function resolveIdentity(overrides) { return { ...DEFAULT_IDENTITY, ...overrides }; }
export function formatIdentityPrompt(identity) {
    const lines = [`You are ${identity.name}.`];
    if (identity.persona)
        lines.push(identity.persona);
    if (identity.capabilities?.length)
        lines.push(`Capabilities: ${identity.capabilities.join(', ')}`);
    return lines.join(' ');
}
export function formatIdentityBadge(identity) { return `${identity.avatar ?? '🤖'} ${identity.name} v${identity.version ?? '?'}`; }
//# sourceMappingURL=identity.js.map
