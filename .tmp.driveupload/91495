/** Exec tool display formatting. */
export function formatExecDisplay(command: string, exitCode: number | null): string { const icon = exitCode === 0 ? '✅' : '❌'; return `${icon} $ ${command} → exit ${exitCode ?? '?'}`; }
