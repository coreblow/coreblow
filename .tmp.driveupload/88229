/**
 * agents/bash-tools.shared.ts
 * Bash tool definitions shared across agent & sandbox modules.
 */
/** Commands that are always blocked regardless of sandbox mode. */
export declare const BLOCKED_COMMANDS: readonly ["rm -rf /", "rm -rf ~", "mkfs", "dd if=/dev/zero", ":(){:|:&};:", "fork()", "> /dev/sda", "chmod -R 777 /", "chown -R"];
/** Commands that are safe to run without approval. */
export declare const SAFE_COMMANDS: readonly ["echo", "cat", "ls", "pwd", "date", "whoami", "uname", "head", "tail", "wc", "sort", "uniq", "grep", "find", "which", "type", "file", "stat", "du", "df"];
/** Check if a command contains any blocked patterns. */
export declare function isBlockedCommand(cmd: string): boolean;
/** Check if a command starts with a safe command. */
export declare function isSafeCommand(cmd: string): boolean;
/** Extract the base command from a full command string. */
export declare function extractBaseCommand(cmd: string): string;
//# sourceMappingURL=bash-tools.shared.d.ts.map
