/**
 * CoreBlow CLI Credentials Manager
 *
 * Manages CLI-level credential storage, retrieval, and secure handling
 * for interactive sessions. Supports keychain integration, env-file based
 * auth, and secure credential prompting.
 *
 * Equivalent: CoreBlow src/agents/cli-credentials.ts (580 LOC)
 */
export interface StoredCredential {
    provider: string;
    key: string;
    name?: string;
    createdAt: number;
    updatedAt: number;
    source: 'interactive' | 'env-file' | 'keychain' | 'config';
}
export interface CredentialStore {
    version: number;
    credentials: StoredCredential[];
    lastModified: number;
}
export interface CredentialPromptResult {
    provider: string;
    key: string;
    save: boolean;
}
/**
 * Get the credentials file path
 */
export declare function getCredentialsPath(homeDir?: string): string;
/**
 * Load credentials from disk
 */
export declare function loadCredentials(filePath?: string): CredentialStore;
/**
 * Save credentials to disk
 */
export declare function saveCredentials(filePath?: string): boolean;
/**
 * Add or update a credential
 */
export declare function setCredential(provider: string, key: string, options?: {
    name?: string;
    source?: StoredCredential['source'];
    save?: boolean;
}): StoredCredential;
/**
 * Get a credential for a provider
 */
export declare function getCredential(provider: string): StoredCredential | undefined;
/**
 * Remove a credential
 */
export declare function removeCredential(provider: string, save?: boolean): boolean;
/**
 * List all stored credentials (with masked keys)
 */
export declare function listCredentials(): Array<{
    provider: string;
    maskedKey: string;
    name?: string;
    source: string;
    updatedAt: number;
}>;
/**
 * Clear all stored credentials
 */
export declare function clearCredentials(save?: boolean): void;
/**
 * Load credentials from a .env file
 */
export declare function loadFromEnvFile(envPath: string): StoredCredential[];
/**
 * Validate stored credential format
 */
export declare function validateCredential(credential: StoredCredential): {
    valid: boolean;
    error?: string;
};
/**
 * Get credential store diagnostics
 */
export declare function getStoreDiagnostics(): {
    totalCredentials: number;
    providers: string[];
    lastModified: number;
    storeVersion: number;
    hasFile: boolean;
};
//# sourceMappingURL=cli-credentials.d.ts.map
