/** Subagent run manager — concurrent execution slots. */
export class RunManager {
    running = new Set();
    maxConcurrent;
    constructor(max = 5) { this.maxConcurrent = max; }
    canRun() { return this.running.size < this.maxConcurrent; }
    start(id) { if (!this.canRun())
        return false; this.running.add(id); return true; }
    stop(id) { this.running.delete(id); }
    count() { return this.running.size; }
    clear() { this.running.clear(); }
}
//# sourceMappingURL=subagent-registry-run-manager.js.map
