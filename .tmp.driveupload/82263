export function resolveHookPolicy(hookName, cfg) {
    const hooks = cfg.hooks;
    if (!hooks)
        return true;
    const disabled = hooks.disabled;
    if (Array.isArray(disabled) && disabled.includes(hookName)) {
        return false;
    }
    return true;
}
//# sourceMappingURL=hooks-policy.js.map
