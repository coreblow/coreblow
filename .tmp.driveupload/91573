/** Model fallback type definitions. */
export type FallbackReason = 'rate_limit' | 'overloaded' | 'error' | 'timeout' | 'manual';
export interface FallbackEntry { from: string; to: string; reason: FallbackReason; timestamp: number; }
