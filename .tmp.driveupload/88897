/**
 * agents/subagent-depth.ts
 * Subagent nesting depth tracking to prevent infinite recursion.
 */
export const MAX_SUBAGENT_DEPTH = 5;
export function createDepthContext(max = MAX_SUBAGENT_DEPTH) { return { current: 0, max, path: [] }; }
export function canSpawnSubagent(ctx) { return ctx.current < ctx.max; }
export function incrementDepth(ctx, agentId) { return { current: ctx.current + 1, max: ctx.max, path: [...ctx.path, agentId] }; }
export function formatDepthPath(ctx) { return ctx.path.length === 0 ? '(root)' : ctx.path.join(' → '); }
export function isAtMaxDepth(ctx) { return ctx.current >= ctx.max; }
//# sourceMappingURL=subagent-depth.js.map
