/**
 * agents/tool-call-id.ts
 * Unique tool call ID generation.
 */
import { randomBytes } from 'node:crypto';
let counter = 0;
export function generateToolCallId(prefix = 'toolu') { return `${prefix}_${Date.now().toString(36)}_${(++counter).toString(36)}_${randomBytes(4).toString('hex')}`; }
export function isValidToolCallId(id) { return typeof id === 'string' && id.length > 0 && id.length < 128; }
export function extractToolCallPrefix(id) { const m = id.match(/^([a-z]+)_/); return m ? m[1] : null; }
//# sourceMappingURL=tool-call-id.js.map
