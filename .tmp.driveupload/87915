/**
 * CoreBlow Config — Defaults
 *
 * CoreBlow equivalent: config/defaults.ts + config/port-defaults.ts + config/talk-defaults.ts
 *                    + config/agent-limits.ts + config/doc-baseline.ts
 * Default configuration values.
 */
import type { CoreBlowConfig, AgentConfig, GatewayConfig, SandboxConfig, ModelsConfig } from './types.js';
export declare const DEFAULT_AGENT_CONFIG: Required<Pick<AgentConfig, 'temperature' | 'maxTokens' | 'timeout' | 'maxTurns' | 'autoCompact' | 'compactThreshold'>>;
export declare const AGENT_LIMITS: {
    maxContextWindow: number;
    maxOutputTokens: number;
    maxSystemPromptLength: number;
    maxTurns: number;
    maxTimeoutSeconds: number;
    minTemperature: number;
    maxTemperature: number;
};
export declare const DEFAULT_GATEWAY_CONFIG: Required<Pick<GatewayConfig, 'port' | 'host'>>;
export declare const DEFAULT_SANDBOX_CONFIG: Required<Pick<SandboxConfig, 'mode' | 'image' | 'network' | 'cpus' | 'memoryMb' | 'idleTimeoutHours' | 'maxAgeDays'>>;
export declare const DEFAULT_MODELS_CONFIG: Partial<ModelsConfig>;
export declare function createDefaultConfig(): CoreBlowConfig;
//# sourceMappingURL=defaults.d.ts.map
