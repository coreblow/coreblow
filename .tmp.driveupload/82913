/**
 * auto-reply/chunk.ts
 * Long message chunking — split by paragraph/sentence boundaries.
 */
/** Split a long message into chunks that respect platform limits. */
export declare function chunkMessage(text: string, platform?: string): string[];
/** Check if a code block is being split mid-block. */
export declare function containsOpenCodeBlock(text: string): boolean;
/** Smart chunk that avoids splitting code blocks. */
export declare function chunkMessageSmart(text: string, platform?: string): string[];
//# sourceMappingURL=chunk.d.ts.map
