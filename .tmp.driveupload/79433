/**
 * web-search/credential-resolver.ts
 * Resolve web search provider credentials from config and env vars.
 * Follows CoreBlow's credential resolution pattern.
 */
import type { WebSearchProviderEntry } from './types.js';
/** Normalize a secret input value — trim and check for empty. */
export declare function normalizeSecretInput(value: unknown): string | undefined;
/** Read the first available env var value from a list. */
export declare function readProviderEnvValue(envVars: string[]): string | undefined;
/** Check if a provider requires credentials. */
export declare function providerRequiresCredential(provider: Pick<WebSearchProviderEntry, 'requiresCredential'>): boolean;
/** Check if a provider entry has valid credentials available. */
export declare function hasProviderCredential(provider: Pick<WebSearchProviderEntry, 'credentialPath' | 'envVars' | 'getConfiguredCredentialValue' | 'getCredentialValue' | 'requiresCredential'>, config: Record<string, unknown> | undefined, searchConfig: Record<string, unknown> | undefined): boolean;
//# sourceMappingURL=credential-resolver.d.ts.map
