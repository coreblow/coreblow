export async function handlePluginList(ctx) {
    return '🔌 Installed plugins:\n  • No plugins installed.\n\nUse /plugin install <name> to install a plugin.';
}
export async function handlePluginInstall(ctx) {
    const name = ctx.command.args.name;
    if (!name)
        return 'Usage: /plugin install <name>';
    return `📦 Installing plugin \`${name}\`...\n✅ Plugin \`${name}\` installed successfully.`;
}
export async function handlePluginEnable(ctx) {
    const name = ctx.command.args.name;
    if (!name)
        return 'Usage: /plugin enable <name>';
    return `✅ Plugin \`${name}\` enabled.`;
}
export async function handlePluginDisable(ctx) {
    const name = ctx.command.args.name;
    if (!name)
        return 'Usage: /plugin disable <name>';
    return `✅ Plugin \`${name}\` disabled.`;
}
export async function handlePluginInfo(ctx) {
    const name = ctx.command.args.name;
    if (!name)
        return 'Usage: /plugin info <name>';
    return `🔌 Plugin: \`${name}\`\n  • Status: not installed\n  • Version: unknown`;
}
export async function handlePluginUninstall(ctx) {
    const name = ctx.command.args.name;
    if (!name)
        return 'Usage: /plugin uninstall <name>';
    return `🗑️ Plugin \`${name}\` uninstalled.`;
}
//# sourceMappingURL=plugin.js.map
