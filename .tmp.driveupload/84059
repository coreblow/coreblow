/**
 * plugin-sdk/entrypoints.ts
 * [PORT] openclaw-main
 *
 * Source of truth for which plugin-sdk subpaths are publicly exported.
 * Consumed by:
 *  - scripts/write-plugin-sdk-entry.mjs  (generates package.json exports)
 *  - build pipeline (drives tsconfig includes for dist output)
 */
export declare const pluginSdkEntrypoints: readonly string[];
export declare const pluginSdkSubpaths: string[];
/** Map every SDK entrypoint name to its source file path inside the repo. */
export declare function buildPluginSdkEntrySources(entries?: readonly string[]): Record<string, string>;
/** List the public package specifiers that should resolve to plugin SDK entrypoints. */
export declare function buildPluginSdkSpecifiers(): string[];
/** Build the package.json exports map for all plugin SDK subpaths. */
export declare function buildPluginSdkPackageExports(): Record<string, {
    types: string;
    default: string;
}>;
/** List the dist artifacts expected for every generated plugin SDK entrypoint. */
export declare function listPluginSdkDistArtifacts(): string[];
//# sourceMappingURL=entrypoints.d.ts.map
