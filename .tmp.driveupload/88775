/** Skill installation output formatting. */
export function formatDownloadProgress(downloaded, total) { const pct = total > 0 ? Math.round((downloaded / total) * 100) : 0; return `Downloading: ${pct}%`; }
//# sourceMappingURL=skills-install-output.js.map
