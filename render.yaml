services:
  - type: web
    name: coreblow
    runtime: node
    plan: starter
    buildCommand: cd gateway && npm ci && npm run build
    startCommand: cd gateway && node dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
