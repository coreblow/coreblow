/** CoreBlow — Control Plane Audit */ export interface AuditEntry { action: string; user: string; timestamp: number; details?: Record<string, unknown>; } const auditLog: AuditEntry[] = []; export function recordAudit(action: string, user: string, details?: Record<string, unknown>): void { auditLog.push({ action, user, timestamp: Date.now(), details }); } export function getAuditLog(): AuditEntry[] { return [...auditLog]; }
