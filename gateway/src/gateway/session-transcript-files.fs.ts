/** CoreBlow — Session Transcript Files */ import fs from "node:fs"; export function appendTranscriptLine(filePath: string, entry: unknown): void { fs.appendFileSync(filePath, JSON.stringify(entry) + "\n"); } export function readTranscriptLines(filePath: string): unknown[] { try { return fs.readFileSync(filePath, "utf8").trim().split("\n").filter(Boolean).map((l) => JSON.parse(l)); } catch { return []; } }
