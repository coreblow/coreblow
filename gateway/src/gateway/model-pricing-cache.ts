/** CoreBlow — Model Pricing Cache */ export interface ModelPricing { model: string; inputPer1kTokens: number; outputPer1kTokens: number; } const cache = new Map<string, ModelPricing>(); export function setModelPricing(pricing: ModelPricing): void { cache.set(pricing.model, pricing); } export function getModelPricing(model: string): ModelPricing | undefined { return cache.get(model); }
