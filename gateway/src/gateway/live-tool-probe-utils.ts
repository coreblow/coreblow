/** CoreBlow — Live Tool Probe Utils */ export async function probeToolEndpoint(url: string): Promise<{ available: boolean; latencyMs: number }> { const start = Date.now(); try { const r = await fetch(url, { method: "HEAD", signal: AbortSignal.timeout(5000) }); return { available: r.ok, latencyMs: Date.now() - start }; } catch { return { available: false, latencyMs: Date.now() - start }; } }
