/** CoreBlow — Open Responses HTTP */ export function createOpenResponsesHandler(): (req: Request) => Promise<Response> { return async (req) => new Response(JSON.stringify({ error: "Not implemented" }), { status: 501 }); }
