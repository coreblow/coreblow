/** CoreBlow — CLI Session History */ export interface HistoryEntry { sessionId: string; query: string; response: string; timestamp: number; model: string; } const history: HistoryEntry[] = []; export function addHistoryEntry(entry: HistoryEntry): void { history.push(entry); } export function getHistory(limit?: number): HistoryEntry[] { return limit ? history.slice(-limit) : [...history]; } export function clearHistory(): void { history.length = 0; }
