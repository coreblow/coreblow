/** CoreBlow — Tools Invoke HTTP */ export interface ToolInvokeRequest { toolName: string; input: Record<string, unknown>; sessionId?: string; } export async function invokeToolViaHttp(baseUrl: string, req: ToolInvokeRequest): Promise<unknown> { const r = await fetch(baseUrl + "/tools/invoke", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(req) }); return r.json(); }
