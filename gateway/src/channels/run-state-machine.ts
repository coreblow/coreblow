/** CoreBlow — Channel State Machine */ export type ChannelState = "disconnected" | "connecting" | "connected" | "error"; export function transitionState(from: ChannelState, event: string): ChannelState { if (event === "connect") return "connecting"; if (event === "connected") return "connected"; if (event === "error") return "error"; if (event === "disconnect") return "disconnected"; return from; }
