/** CoreBlow — Transport Stall Watchdog */ export class StallWatchdog { private timer: ReturnType<typeof setTimeout> | null = null; constructor(private timeoutMs = 30000, private onStall: () => void) {} start(): void { this.timer = setTimeout(() => this.onStall(), this.timeoutMs); } reset(): void { if (this.timer) clearTimeout(this.timer); this.start(); } stop(): void { if (this.timer) clearTimeout(this.timer); this.timer = null; } }
