/** CoreBlow — Control UI Assets */ import fs from 'node:fs'; import path from 'node:path'; export function resolveControlUiDir(baseDir: string): string { return path.join(baseDir, 'control-ui'); } export function hasControlUiAssets(baseDir: string): boolean { const dir = resolveControlUiDir(baseDir); try { return fs.existsSync(path.join(dir, 'index.html')); } catch { return false; } } export function getControlUiAssetPath(baseDir: string, assetPath: string): string { return path.join(resolveControlUiDir(baseDir), assetPath); }
