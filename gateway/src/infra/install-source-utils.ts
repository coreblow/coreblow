/** CoreBlow — Install Source Utils */ export type InstallSource = "npm" | "git" | "local" | "url"; export function detectInstallSource(spec: string): InstallSource { if (spec.startsWith("http://") || spec.startsWith("https://")) return "url"; if (spec.startsWith("git://") || spec.startsWith("git+")) return "git"; if (spec.startsWith("/") || spec.startsWith("./") || spec.startsWith("../")) return "local"; return "npm"; }
