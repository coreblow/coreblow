/** CoreBlow — Push APNS Relay */ export interface ApnsRelayConfig { relayUrl: string; apiKey: string; } export async function relayApnsPush(config: ApnsRelayConfig, deviceToken: string, payload: Record<string, unknown>): Promise<boolean> { try { const r = await fetch(config.relayUrl, { method: "POST", headers: { "Content-Type": "application/json", Authorization: "Bearer " + config.apiKey }, body: JSON.stringify({ deviceToken, payload }) }); return r.ok; } catch { return false; } }
