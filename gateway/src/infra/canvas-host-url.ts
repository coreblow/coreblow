/** CoreBlow — Canvas Host URL */ export function resolveCanvasHostUrl(port: number, host = 'localhost'): string { return `http://${host}:${port}`; } export function parseCanvasHostUrl(url: string): { host: string; port: number } | null { try { const u = new URL(url); return { host: u.hostname, port: parseInt(u.port, 10) || (u.protocol === 'https:' ? 443 : 80) }; } catch { return null; } }
