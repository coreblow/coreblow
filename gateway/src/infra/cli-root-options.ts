/** CoreBlow — CLI Root Options */ export interface CliRootOptions { verbose?: boolean; configPath?: string; profile?: string; debug?: boolean; json?: boolean; } export function parseRootOptions(argv: string[]): CliRootOptions { const opts: CliRootOptions = {}; for (const arg of argv) { if (arg === '--verbose' || arg === '-v') opts.verbose = true; if (arg === '--debug') opts.debug = true; if (arg === '--json') opts.json = true; if (arg.startsWith('--config=')) opts.configPath = arg.slice(9); if (arg.startsWith('--profile=')) opts.profile = arg.slice(10); } return opts; }
