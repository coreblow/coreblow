/** CoreBlow — Clipboard */ import { execFileSync } from 'node:child_process'; export function copyToClipboard(text: string, platform = process.platform): boolean { try { const cmd = platform === 'darwin' ? 'pbcopy' : platform === 'win32' ? 'clip' : 'xclip'; execFileSync(cmd, { input: text, timeout: 3000, stdio: ['pipe', 'ignore', 'ignore'] }); return true; } catch { return false; } }
