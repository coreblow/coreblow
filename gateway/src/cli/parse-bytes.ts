/** CoreBlow — Parse Bytes */ export function parseBytes(input: string): number | null { const match = /^(\d+(?:\.\d+)?)\s*(b|kb|mb|gb|tb)?$/i.exec(input.trim()); if (!match) return null; const n = parseFloat(match[1]); const unit = (match[2] || "b").toLowerCase(); const mul: Record<string, number> = { b: 1, kb: 1024, mb: 1048576, gb: 1073741824, tb: 1099511627776 }; return Math.round(n * (mul[unit] ?? 1)); }
