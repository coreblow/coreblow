/** CoreBlow — Parse Duration */ export function parseDuration(input: string): number | null { const match = /^(\d+)\s*(ms|s|m|h|d)?$/i.exec(input.trim()); if (!match) return null; const n = parseInt(match[1], 10); const unit = (match[2] || "ms").toLowerCase(); const mul: Record<string, number> = { ms: 1, s: 1000, m: 60000, h: 3600000, d: 86400000 }; return n * (mul[unit] ?? 1); }
