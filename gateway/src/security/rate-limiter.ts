export class RateLimiter { private hits = new Map<string, number[]>(); constructor(private max: number = 100, private windowMs: number = 60000) {} check(key: string): boolean { const now = Date.now(); const arr = (this.hits.get(key) || []).filter(t => now - t < this.windowMs); if (arr.length >= this.max) return false; arr.push(now); this.hits.set(key, arr); return true; } reset(key: string) { this.hits.delete(key); } }
