/** CoreBlow — Zod Schema: Agent Model */ import { validateString, validateNumber } from "./zod-schema.core.js"; export function validateAgentModel(config: Record<string, unknown>): string[] { const errors: string[] = []; errors.push(...validateString(config.name, "name")); if (config.maxInputTokens !== undefined) errors.push(...validateNumber(config.maxInputTokens, "maxInputTokens", 1)); return errors; }
