/** CoreBlow — Zod Schema: Session */ import { validateNumber } from "./zod-schema.core.js"; export function validateSessionConfig(config: Record<string, unknown>): string[] { const errors: string[] = []; if (config.maxIdleMs !== undefined) errors.push(...validateNumber(config.maxIdleMs, "maxIdleMs", 0)); if (config.maxDurationMs !== undefined) errors.push(...validateNumber(config.maxDurationMs, "maxDurationMs", 0)); return errors; }
