/** CoreBlow — Zod Schema: Hooks */ import { validateString, validateEnum } from "./zod-schema.core.js"; export function validateHookConfig(config: Record<string, unknown>): string[] { const errors: string[] = []; errors.push(...validateString(config.name, "hook.name")); errors.push(...validateEnum(config.trigger, "hook.trigger", ["message:before", "message:after", "session:start", "session:end"])); return errors; }
