/** CoreBlow — Talk Config */ import { DEFAULT_TALK, type TalkDefaults } from "./talk-defaults.js"; export function resolveTalkConfig(config: Record<string, unknown>): TalkDefaults { return { model: String(config.model ?? DEFAULT_TALK.model), temperature: Number(config.temperature ?? DEFAULT_TALK.temperature), maxTokens: Number(config.maxTokens ?? DEFAULT_TALK.maxTokens), systemPrompt: config.systemPrompt as string | undefined }; }
