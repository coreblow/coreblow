/** CoreBlow — Zod Schema: Providers Core */ import { validateString } from "./zod-schema.core.js"; export function validateProviderConfig(config: Record<string, unknown>): string[] { const errors: string[] = []; errors.push(...validateString(config.name, "provider.name")); if (config.apiKey !== undefined) errors.push(...validateString(config.apiKey, "provider.apiKey")); return errors; }
