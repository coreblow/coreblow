/** CoreBlow — Zod Schema: Agent Defaults */ import { validateNumber } from "./zod-schema.core.js"; export function validateAgentDefaults(config: Record<string, unknown>): string[] { const errors: string[] = []; if (config.maxTurns !== undefined) errors.push(...validateNumber(config.maxTurns, "maxTurns", 1, 100)); if (config.timeout !== undefined) errors.push(...validateNumber(config.timeout, "timeout", 1000, 600000)); return errors; }
