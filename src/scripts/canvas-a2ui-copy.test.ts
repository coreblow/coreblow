import { describe, it, expect } from "vitest";

describe("canvas-a2ui-copy", () => {
  it("module exists (stub — source file mapping pending)", () => {
    expect(true).toBe(true);
  });

  it.todo("throws a helpful error when assets are missing");
  it.todo("skips missing assets when COREBLOW_A2UI_SKIP_MISSING=1");
  it.todo("copies bundled assets to dist");
});
