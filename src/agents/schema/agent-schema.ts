/** CoreBlow — Agent Schema */ export interface AgentSchema { id: string; name: string; version: string; model: string; provider: string; capabilities: string[]; tools: string[]; } export function validateAgentSchema(schema: Record<string, unknown>): string[] { const errors: string[] = []; if (!schema.id) errors.push("Missing id"); if (!schema.name) errors.push("Missing name"); return errors; }
