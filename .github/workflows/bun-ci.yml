# CoreBlow — Bun Compatibility Check
# Updated: 2026-04-24 — Single source tree (gateway/ removed)

name: Bun CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  bun-test:
    name: Bun Compatibility
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      matrix:
        bun-version: ['1.1', '1.2']
    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: pnpm

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: ${{ matrix.bun-version }}

      - run: pnpm install --frozen-lockfile
      - run: bun run typecheck
      - run: timeout 5m bun test || echo "Bun test compatibility check failed or timed out (non-blocking)"
        continue-on-error: true

  bun-bench:
    name: Bun Benchmarks
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: bun-test
    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: pnpm

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: '1.2'

      - run: pnpm install --frozen-lockfile
      - name: Startup Time
        run: |
          echo "Measuring cold-start time..."
          time bun run src/cli/init.ts --help 2>/dev/null || true
