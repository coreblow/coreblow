import SwiftUI

struct ToolOutputView: View {
    var body: some View {
        NavigationStack {
            VStack {
                Text("ToolOutputView")
            }
            .navigationTitle("ToolOutput") 
        }
    }
}
