import SwiftUI

struct ConversationListView: View {
    var body: some View {
        NavigationStack {
            VStack {
                Text("ConversationListView")
            }
            .navigationTitle("ConversationList")
        }
    }
}
