import SwiftUI

struct AgentView: View {
    var body: some View {
        NavigationStack {
            VStack {
                Text("AgentView")
            }
            .navigationTitle("Agent")
        }
    }
}
