import SwiftUI

struct AboutView: View {
    var body: some View {
        NavigationStack {
            VStack {
                Text("AboutView")
            }
            .navigationTitle("About")
        }
    }
}
