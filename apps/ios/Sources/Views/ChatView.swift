import SwiftUI

struct ChatView: View {
    var body: some View {
        NavigationStack {
            VStack {
                Text("ChatView")
            }
            .navigationTitle("Chat") 
        }
    }
}
