import SwiftUI

struct SidebarView: View {
    var body: some View {
        NavigationStack {
            VStack {
                Text("SidebarView")
            }
            .navigationTitle("Sidebar")
        }
    }
}
