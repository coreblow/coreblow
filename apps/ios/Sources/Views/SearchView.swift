import SwiftUI

struct SearchView: View {
    var body: some View {
        NavigationStack {
            VStack {
                Text("SearchView")
            }
            .navigationTitle("Search")
        }
    }
}
