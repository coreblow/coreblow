import SwiftUI

struct ContentView: View {
    var body: some View {
        NavigationStack {
            VStack {
                Text("ContentView")
            }
            .navigationTitle("Content")
        }
    }
}
